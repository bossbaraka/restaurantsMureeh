# TEST 4 — FINAL PRODUCTION & UX READINESS GATE (تقرير الجاهزية النهائية قبل الإطلاق)
**المشروع:** restaurantsMureeh (MÉRAR SaaS) · **التاريخ:** 2026-09-19 · **الأدوار:** Principal Frontend Engineer / UX Engineer / Production Reliability Engineer / Mobile Web QA Engineer / SaaS Launch Readiness Auditor
**البيئة:** Node 22 · بناء إنتاجي حقيقي (`dist/`) مخدوم عبر Express · PostgreSQL 16 مضمّن (:5433) مع 17/17 ترحيلات + بذرة 3 مستأجرين/10 مستخدمين/5 طاولات/طلبات · خادم API يعمل على :3001 · سلسلة build→lint→test كاملة مع قاعدة حقيقية.
**المنهجية:** تدقيقFACTS فقط — لا تعديلات. بناء/فحص/اختبارات إنتاجية، فحوص runtime مباشرة (health/SPA/404/رفع صورة/إعادة تشغيل خادم/إسقاط قاعدة البيانات/حراس الإقلاع fail-closed/بث SSE)، تدقيق ثابت لكل شاشات الزبون والحاقنات والوصولية والأداء، وتوليف رحلات المستخدمين من TEST 3 (104 فحص E2E) + TEST 2 (113/114 أمني) + TEST 1.

---

## الحكم النهائي: **CONDITIONAL PASS** — جاهز للإطلاق بعد قرار نطاق واحد وإصلاح واحد

المنتج **مبني بصلابة إنتاجية نادرة**: بناء نظيف، اختبارات خضراء، حراس تهيئة fail-closed مثاليون، تدهور رشيق عند سقوط القاعدة، وفصل حالات UX للزبون نموذجي (Loading/Empty/Error/Offline/Invalid لا تنهار في خطأ عام). **كل شيء يعمل لخدمة الطاولات من QR حتى الإيصال.**

المتبقي عائقان موروثان من TEST 3 لم يُصلحا (لم يُسمح بالإصلاح في هذه السلسلة): **بيع الكاونتر Walk-in معطّل (P1-A)** و**البث اللحظي لا يصل للمتصفحات (P1-B)** — وكلاهما موثق بأدلة قاطعة أدناه. لا يوجد أي P0.

---

## 1) مصفوفة الإنتاج (Production Matrix)

| المحور | النتيجة | الأدلة (اليوم، على بناء اليوم) |
|---|---|---|
| **Build** | ✅ PASS | `npm run build` خروج 0 · `dist/` كامل: index.html بـ `lang="ar" dir="rtl" class="dark"` + `viewport-fit=cover` + PWA meta + `theme-color #020A14` + `_redirects` + أيقونات · chunk واحد **998.5 kB JS (243 kB gzip)** + **195.4 kB CSS (34 kB gzip)** — لا أصول مفقودة ولا استيرادات مكسورة |
| **Lint** | ❌ FAIL | **خطأان** `react-hooks/rules-of-hooks` (استدعاء شرطي لـ useState/useEffect في `CustomerLayout.tsx`) + **147 تحذيراً** — نفس F-01 من TEST 1، لم يُصلح. بوابة CI حمراء |
| **Tests (افتراضي)** | ✅ PASS | `npm test` بلا DATABASE_URL: **خروج 0 — 57 ملفاً ناجحاً / 3 متخطاة** (المتخطاة: ملفات DB-gated) |
| **Tests (قاعدة حقيقية)** | ✅ PASS (من جهة المنتج) | بـ DATABASE_URL + SAAS_E2E_SEED/MUTATE: كل ملفات التكامل الحقيقية تنجح (payment-receipt-scope 8/8، prismaPostgresValidation، theme-image-persistence، payment-proof-flow **10/10** عند ضبط JWT_SECRET) — الإخفاقات التسعة الوحيدة هي **عيوب في الاختبارات نفسها** (F-02: 8× `body.id` في product-image-persistence + 1× محلّل SQL للتعليقات في transfer-details) وليست في المنتج |
| **Runtime** | ✅ PASS | `/api/health` → `{"status":"ok"}` · SPA `/r/test-tenant-a` → 200 HTML · `GET /api/nope` → **404 JSON** مقروء آلياً (تحفّظ README القديم عن ابتلاع SPA لمسارات /api **لم يعد يُستنسخ**) |
| **Database** | ✅ PASS | 17/17 ترحيل تُطبَّق بنظافة على PG16 فارغ · **إسقاط القاعدة عمداً**: القائمة → 500 برسالة عربية نظيفة بلا تسريب stack، تسجيل الدخول → 500 عربي، **العملية تبقى حية** و`/` يستمر 200، وعند عودة القاعدة **الاسترداد فوري** (200 في الطلب التالي) |
| **Storage** | ✅ PASS | رفع صورة (kind=gallery) عبر تسجيل مدير حقيقي → 200؛ GET الأصل → 200 `image/png`؛ **بعد إيقاف الخادم بالكامل وإعادة إقلاعه → الأصل ما زال 200** (المشغّل المحلي disk-backed) · **حراس fail-closed مُتحقق منهم عملياً**: (أ) production + local بدون opt-in → رفض إقلاع برسالة إرشادية كاملة (config.ts:214)؛ (ب) supabase بلا مفاتيح → رفض إقلاع (config.ts:196)؛ (ج) JWT أقل من 32 حرفاً → رفض؛ (د) production بلا CORS_ORIGIN دقيق → رفض (config.ts:150) |
| **Realtime** | ❌ FAIL للمتصفحات | SSE على بناء اليوم: `Accept-Encoding: identity` → **101 بايت فوراً** (حدث اتصال سليم)؛ `gzip` (الافتراضي لكل المتصفحات) → **0 بايت** — P1-B مؤكد على البناء الحالي. **التخفيف**: استطلاع خلفي كل 10 ثوانٍ بكل الشاشات (استبدل استطلاع 1.5ث×8 نقاط=320 طلب/دقيقة السابق) → التحديثات تصل خلال ≤10 ثوانٍ رغم انقطاع SSE |

**ملاحظة تشغيلية:** `render.yaml` سليم — الترحيلات في startCommand، مسار health صحيح، Supabase fail-closed، TRUST_PROXY=1. `/api/health` لا يفحص القاعدة (خيار مقصود لتجنب تسريب معلومات) — **نقطة عمياء للمراقبة**: خادم بقاعدة ميتة يبدو "صحياً" لـ Render ولن يُعاد تشغيله (P3).

---

## 2) مصفوفة تجربة الاستخدام (UX Matrix)

| الرحلة | النتيجة | الأدلة |
|---|---|---|
| **Customer (QR→قائمة→طلب→تتبع→حساب)** | ✅ PASS | آلة حالات الدخول (`services/customerEntry.ts` + `CustomerLayout.tsx`) **نموذجية الفصل**: LOADING/RETRYING (3 محاولات، 500/1000ms)/RECOVERY/INVALID(qr│restaurant) حالات متميزة · خطأ الشبكة = عابر دائماً (لا لوم على الزبون أبداً) · 404-مع-جلسة = عابر · حماية stale-run · **READY فقط بعد نجاح حقيقي للجلسة والكتالوج → وميض "قائمة فارغة" مستحيل بنيوياً** · شاشات مخصصة: SUSPENDED، وQR-مطلوب، وبوابة تحميل بصرية بمدة دنيا |
| **Staff (دخول→لوحة→KDS→تقديم→نداءات)** | ✅ PASS | TEST 3: دورة المطبخ كاملة مع بوابة الدفع-قبل-التنفيذ · LoginModal: `role=dialog aria-modal aria-label`، كل أزرار الإرسال `disabled` أثناء isLoading والقفل، حقول موصوفة aria-label، **إعادة تحقق 401 عند الإقلاع 6/6** (TEST 2) · KDS يعتمد على SSE + استطلاع 10ث |
| **Cashier (دخول→POS→طلب→دفع→إيصال)** | ⚠️ CONDITIONAL | خدمة الطاولات كاملة حتى الإيصال (TEST 3: تسلسل RC-2026-XXXX بلا فجوات، إبطال، إعادة تحصيل) · **بيع الكاونتر Walk-in → 500** (P1-A) |
| **Mobile (360–1440px)** | ✅ PASS (تدقيق كود/CSS) | 33 استعلام وسائط (640/1100/max-640) · لا أنماط عرض ثابت كبيرة · `viewport-fit=cover` + مساحات أمان · أهداف لمس 44px في 18 موضعاً · **حد النزاهة:** لا متصفحاً حقيقياً في هذه البيئة — التحقق بكسلي عند 360/390/414 غير ممكن هنا؛ الفحوص الديناميكية (TEST 3) عبر SPA حقيقية لم تُظهر أي كسر |
| **RTL/عربية** | ✅ PASS | `dir="rtl"` و`lang="ar"` من مستند HTML نفسه (ليس runtime فقط) · كل النصوص عربية أصلية · أخطاء عربية صادقة من الخادم («حدث خطأ في استرجاع قائمة الطعام») |
| **Accessibility** | ✅ PASS (عملي) | قاعدة focus-visible عالمية `:where(a,button,input,select,textarea,summary,[tabindex])` · حوارات: `role=dialog aria-modal aria-labelledby` (ProductDetailModal، CartDrawer، LoginModal) · 226 مرجع aria · 31 مرجع reduced-motion · تباين luxury-400 على 900 ≈ **5.7:1 (AA)** · لا فشل مادي مكتشف في التدقيق الثابت |
| **Error Recovery** | ✅ PASS | لكل فشل مسار تعافٍ: شبكة → RETRYING/RECOVERY بحدود؛ 404 QR → شاشة INVALID صريحة؛ 403 مطعم → INVALID؛ 500 قائمة → عابر (زر إعادة محاولة)؛ SSE → backoff 1s→30s+jitter + استطلاع؛ 401 → إعادة تحقق إقلاع؛ القاعدة ميتة → الخادم حي + استرداد فوري · `api.ts` مهلة 30ث (صياغة "database" لخطأ الشبكة 503 خفيفة التضليل — P3) |

---

## 3) النتائج الحرجة (Critical Findings)

### P0 — لا شيء 🎉

### P1 (عائقة إطلاق — موروثتان من TEST 3، مؤكدتان على بناء اليوم)

**P1-A — بيع الكاونتر (Walk-in) في POS يعطي 500 دائماً**
- **المسار:** `POST /api/orders` بقيمة `tableId: "__WALKIN__"`
- **الملفات:** `server/routes/manager.ts:705` (القيمة الحرفية تُمرَّر كـ UUID فيضع FK `Order_tableId_fkey`) + `src/components/manager/CashierPOSView.tsx:398/:231` (يرسل القيمة الحرفية)
- **الملاحظ:** `500` — خرق قيد قاعدة البيانات؛ لا يمكن إنشاء أي طلب كاونتر عبر POS
- **المتوقع:** طلب `WALK_IN` بدون طاولة يُنشأ ويعمل كتذكرة كاشير عادية (أو أن يُحظر الخيار بصدق)
- **الدليل:** TEST 3 §POS (خطأ Prisma FK نصاً)؛ `pos.test.ts` كان mocked فلم يلتقطها

**P1-B — بث SSE لا يصل أي متصفح حقيقي (gzip → 0 بايت)**
- **المسار:** `GET /api/public/events?...`
- **الملاحظ (اليوم):** identity → 101 بايت فوراً؛ gzip → **0 بايت** (البث "متصل" لكن لا حدث يفك ضغطه/يعبره)
- **الأثر:** كل شاشات التحديث اللحظي (تتبع الزبون، KDS، نداءات النادل) تعتمد فعلياً على الاستطلاع كل 10 ثوانٍ — **تعمل بلا تحديث يدوي لكن بتأخير ≤10ث بدل اللحظية**
- **المتوقع:** الأحداث تصل مع gzip كما تصل مع identity

### P2 (قبل الإطلاق إن أمكن — لا تمنع)

1. **بوابة lint حمراء** — خطأا rules-of-hooks (F-01) + 147 تحذيراً؛ لا يمنعان التشغيل لكنهما يكسران أي CI gate ويخفيان انحدارات مستقبلية.
2. **حزمة JS واحدة 998.5 kB** (F-03) — لا تقسيم كود؛ شاشة الزبون الأول (QR) تحمّل كود الإدارة والكاشير كاملاً. 243 kB gzip مقبول تقنياً لكنه أول شاشة على 3G جوال.
3. **9 اختبارات persist-ence معطوبة (F-02)** — عيوب في الاختبارات (`body.id` بدل الحِمل الصحيح + محلّل تعليقات SQL)، أي **تغطية عقود الصور/الترحيل معطلة فعلياً** رغم أن السلوك نفسه تحقق يدوياً (رفع/إعادة تشغيل/ثبات).
4. **F-04 (نافذة دفع الزبون في live-preview)** — مسار الإثبات عبر API يعمل (TEST 2 P08)؛ العطل كان في حاضنة العرض؛ لم يُعاد التحقق البصري (لا متصفح هنا). يبقى "غير محسوم بصرياً".

### P3 (تحسينات لاحقة)

- `/uploads/*` يُخدَم بـ `cache-control: public, max-age=0` — **صور القائمة لا تتخزن مؤقتاً في المتصفح إطلاقاً**؛ كل تصفح يعيد تنزيل كل صورة (تخفيفه سطر واحد؛ في مسار Supabase الإنتاجي التخزين المؤقت من CDN مختلف).
- `/api/health` لا يفحص القاعدة → مراقبة Render عمياء عن انقطاع DB.
- رسالة 503 لخطأ الشبكة في `api.ts` تذكر "قاعدة البيانات" — تضليل خفيف للتشخيص.
- `strict:false` في tsconfig (سطح خطأ أوسع مما ينبغي).

---

## 4) عوائق الإطلاق الحقيقية (Launch Blockers فقط)

1. **P1-A (Walk-in POS 500)** — إذا كان بيع الكاونتر ضمن نطاق الإطلاق فهذا **مانع قاطع**؛ إن أُجّل كنطاق فيجب إخفاء الخيار من POS حتى يُصلح (قرار منتج، لا إصلاح تقني).
2. **P1-B (SSE gzip)** — ليس مانعاً بمعنى "لا يعمل" (الاستطلاع يعوض)، لكنه **مانع لمتطلب "لحظي" معلن**؛ قرار: إما إصلاح قبل الإطلاق أو إعلان النطاق "شبه لحظي (≤10ث)" صراحة.

> الباقي كله (lint، الحزمة، الاختبارات المعطوبة، max-age=0) — **ليس عوائق إطلاق**.

---

## 5) رحلات المستخدمين النهائية — نقاط الاحتكاك المرصودة

**الزبون:** QR→تحميل فاخر→ترحيب مرة واحدة لكل تبويب→قائمة: **سلسة بلا احتكاك مكتشف**. التتبع لحظي 10ث (احتكاك P1-B). نداء النادل/الحساب: يعمل مع تهدئة 45ث ضد الإزعاج (سلوك مقصود). الدفع بالتحويل: يعمل عبر API؛ تجربة النافذة البصرية غير محسومة (F-04).
**الطاقم:** دخول PIN/بريد بقفل ومهل، لوحة، KDS ببوابة دفع-قبل-التنفيذ — **بلا احتكاك** (TEST 3: 15/15). تحديث KDS كل ≤10ث.
**الكاشير:** خدمة الطاولات ممتازة حتى الإيصال والإبطال. **الاحتكاك الوحيد الحرج: زر الكاونتر ينفجر 500** (P1-A).

---

## 6) حدود التحقق (ما لم يمكن اختباره هنا — بإخلاص)

- **رندر بكسلي حقيقي** عند 360/390/414/768/1024/1440 وأجهزة لمس فعلية — لا متصفح في البيئة؛ الاستنتاج من تدقيق CSS/كود + SPA فحوص TEST 3.
- **Supabase Storage حقيقي** (مفاتيح غير متوفرة) — تحققنا من الحراس fail-closed ومسار local بالكامل؛ مسار supabase نفسه مغطى بوحدات محاكاة فقط.
- **نشر Render حقيقي** (startup/الترحيل عند الإقلاع) — render.yaml سليم قراءةً؛ الترحيلات مُتحقق منها يدوياً 17/17 على PG16 نظيف.
- **F-04 بصرياً** — انظر أعلاه.

---

## 7) بيانات القرار النهائية (FINAL DECISION DATA)

```
PRODUCTION_BUILD=PASS              # exit 0; 998.5kB JS/243kB gz + 195.4kB CSS/34kB gz; SPA+PWA meta سليم
AUTOMATED_TESTS=PASS              # default 57 files green exit 0; real-DB: كل شيء ينجح إلا 9 عيوب اختبارات (F-02)؛ lint FAIL منفصل (2 errors/147 warnings)
SECURITY_GATE=CONDITIONAL_PASS    # TEST 2: 113/114 (SEC-01..07)
E2E_GATE=CONDITIONAL_PASS         # TEST 3: 102/104
UX_GATE=CONDITIONAL_PASS          # فصل حالات نموذجي؛ التتبع لحظي-10ث؛ mobile/RTL/a11y بلا فشل مادي مكتشف
STORAGE_GATE=PASS                 # ثبات عبر إعادة التشغيل + حراس fail-closed مُتحقق عملياً (Supabase الحقيقي غير مختبر — لا مفاتيح)
REALTIME_GATE=FAIL                # SSE 0 بايت مع gzip لكل المتصفحات؛ الاستطلاع 10ث يعوّض
CRITICAL_BLOCKERS=2               # P1-A Walk-in POS 500؛ P1-B SSE gzip
HIGH_RISKS=5                      # lint أحمر (F-01)؛ 9 اختبارات coverage معطوبة (F-02)؛ حزمة واحدة 998.5kB (F-03)؛ health بلا فحص DB؛ صور بلا تخزين مؤقت (max-age=0)
```

### القرار
**CONDITIONAL PASS — جاهز للإطلاق فور:**
1. إصلاح **P1-A** أو حظر خيار الكاونتر من واجهة POS (قرار منتج بخيار واحد)، **و**
2. إصلاح **P1-B** أو اعتماد "≤10 ثوانٍ" كبند نطاق معلن للتحديثات.

بعد أيٍّ من الخيارين: البنية (بناء/اختبارات/قاعدة/تخزين/تدهور رشيق/أمان) **جاهزة إنتاجياً بمعايير عالية**.

---
*أدلة حية: `/tmp/t4-{build,lint,test,test-clean2,test-default}.log` · أصل الثبات: `/uploads/restaurants/c40e4554-8653-4d29-a303-4b8fd769ff79/gallery/329a9f2e-a43f-4721-ba5b-6587667b7664.png` · سلاسل التقارير: TEST1/TEST2/TEST3_*_REPORT_2026-09-18.md*
