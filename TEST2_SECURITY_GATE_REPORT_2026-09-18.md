# TEST 2 — SECURITY & MULTI-TENANT ISOLATION GATE
**المشروع:** restaurantsMureeh (MÉRAR SaaS) · **التاريخ:** 2026-09-18 · **المُقيّم:** Principal AppSec / SaaS Multi-Tenant Security Review
**البيئة:** Node 22 + Express + Prisma (PostgreSQL 16) · بيانات اختبار مُعاد توليدها لكل تشغيلة · 3 مستأجرين (A نشط، B نشط، C معلَّق→نشط) + 7 أدوار
**منهجية:** 114 فحصاً ديناميكياً عدائياً (probe) عبر HTTP مباشرة + مراجعة ثابتة لكل مسار حساس + تحقق DB مباشر (pg) + مسح أسرار للملفات المتعقَّبة في Git.

---

## الحكم النهائي

| البند | النتيجة |
|---|---|
| مصادقة وإدارة هوية | ✅ سليمة (16/16) |
| RBAC لكل الأدوار | ✅ سليمة (15/15) |
| عزل المستأجرين A↔B | ✅ **لم يُخترق في أي اتجاه** (30/30) |
| QR وجلسات العملاء الزائرين | ✅ سليمة (11/11 QR + 17/17 Session) |
| سلامة المدفوعات | ✅ سليمة (8/8) |
| تحقق المدخلات وتصلب API | ✅ سليمة (11/11) |
| سجلات التدقيق | ✅ سليمة (3/3) + حجب الرموز من اللوقز |
| مقاومة التخمين (Brute Force) | ✅ محددات تعمل (Login 429، PIN 429) |
| قناة الأحداث اللحظية (SSE) | ❌ **عطل تسليم P1 (SEC-01)** — العزل قائم لكن التسليم معطّب |
| مسح الأسرار | ⚠️ لا أسرار حية؛ أدلة نشر تحوي قيم جاهزة للصق (SEC-02) |

**النتيجة الإجمالية: CONDITIONAL PASS** — لا توجد أي ثغرة حرجة في العزل أو التصريح أو المصادقة؛ يوجد عيب واحد High (تعطّل التسليم اللحظي) وبنود تحصين متوسطة/منخفضة.

---

## الفحوصات الديناميكية — ملخص النتائج

**Probe النهائي: 113/114 ناجح** (الإخفاق الوحيد مقصود: توثيق عيب SEC-01) + فحص منفصل لتخمين PIN (429 بعد 11 محاولة فاشلة = نجاح). السجلات الكاملة: `/tmp/sec-probe-final.log`, `/tmp/sec-probe-2.log`, `/tmp/sec-probe-1.log`.

### المرحلة 1 — المصادقة (A01–A16): 16/16 ✅
- كلمة مرور خاطئة/مستخدم غير موجود → 401 برسائل عربية موحّدة (لا كشف حالة الحساب عبر توقيت/نص).
- JWT: توقيع صحيح لكن `role=PLATFORM_ADMIN` مزوَّر → **403** (الخادم يعيد قراءة الدور من DB، لا يثق بحمولة التوكن). `restaurantId=B` مزوَّر في التوكن → تُجبَر الهوية على مستأجر التوكن الأصلي؛ صفر بايتات من B.
- `alg=none`، توكن منتهي، توكن موقَّع بسر آخر → 401. **الإبطال (logout) يعمل عبر tokenVersion**: توكن صادر قبل الخروج يُرفض فوراً.
- مستخدم SUSPENDED → 403. أخطاء المصادقة موحّدة عربياً بدون stack trace.

### المرحلة 2 — RBAC (R01–R15): 15/15 ✅
- كاشير → تغيير خطة/لوحة إحصاءات/حذف منتج → 403/401 حسب الطبقة (مرفوض دائماً).
- ترقية دور ذاتي إلى PLATFORM_ADMIN عبر PATCH → مرفوض.
- موظف مطبخ CAN تغيير حالة الطلب (مسموح تصميماً)؛ STAFF→settle مرفوض؛ سجلات التدقيق = PLATFORM_ADMIN فقط (مدير مطعم → 401/403).
- كل التحقق من الخادم (server-side) — حرس الواجهة غير معتمد عليه إطلاقاً.

### المرحلة 3 — عزل المستأجرين (T01–T30): 30/30 ✅ — **النتيجة المحورية**
مدير A (توكن صحيح) حاول الوصول لكل موارد B عبر المعرفات المباشرة ومعرفات بديلة (query/body/route):
- **قراءة**: طلبات B، مدفوعات B، فروع B، طاولات B، تحليلات B → إما 404 موحّد أو **صفر بايتات من B** (scoped to A).
- **كتابة/تعديل/حذف**: منتج B، طاولة B، طلب B، waiter-request B، عرض B، موظف B، دفع B، فروع B → 403/404/409.
- **الحالة الحرجة T25**: `POST /branches` بـ `restaurantId=B` من مدير A أعاد 201 — تحقق DB مباشر أثبت أن الصف هبط في **مستأجر A نفسه** (getTenantId يجبر نطاق التوكن). ليس تسريباً. T29/T30: صفر صفوف Table/Branch في B بعد كل المحاولات.
- نمط الكود المتبع في كل المسارات: **التصفية بالنطاق داخل استعلام Prisma نفسه** (`where: { id, restaurantId }`) وليس "اعثر ثم تحقق" — غير قابل للتجاوز.

### المرحلة 4 — QR (Q01–Q11): 11/11 ✅
توكن QR غير القابل للتخمين (CSPRNG)؛ توكن معدَّل/من مستأجر C المعلق → 403/404؛ إنشاء الجلسة **يُلزم الجلسة بطاولة التوكن نفسها** (تحقق DB: session.tableId === tableA1.id)؛ لا يمكن استئناف جلسة طاولة أخرى عبر `resumeSessionToken`.

### المرحلة 5 — جلسات الزوار وSSE (S01–S17 + SEC-SSE): عزل 17/17، تسليم ❌
- طلب منتجات B بجلسة A → 400/404. أسعار العميل (`price`, `totalPrice` في items) **تُتجاهل كلياً** — التسعير من DB (الإجمالي=20 من الخادم).
- `clientRequestId` نفسه → 200 مع `replayed:true` (idempotency بدون تذكرة مكررة).
- إلغاء/ملاحظات/نداء نادل على طاولة/طلب مستأجر آخر → 403.
- SSE: بدون اعتماديات → 403؛ توكن موظف مزوَّر → 403؛ بث A لا يحتوي **أي** معرف/حدث من B (S17).
- **SEC-SSE (عيب موثق أدناه)**: مع `Accept-Encoding` الافتراضي للمتصفحات (gzip) يفتح البث لكن **لا يصل أي بايت** — انظر SEC-01.

### المرحلة 6 — المدفوعات (P01–P08): 8/8 ✅
- دفع مزدوج → 409. مبلغ سالب/ناقص → 400. إثبات دفع متعدد الأجزاء cross-tenant (جلسة A على طلب B) → **403** (P07). JSON بدون ملف → 400 (رفض صالح، P06).
- إثبات على طلب نفسه (sanity) → 201 + تسجيل PAYMENT_PROOF_SUBMITTED. الإيصالات في namespace خاص غير عام.

### المرحلة 7 — تحقق المدخلات وتصلب API (I01–I11): 11/11 ✅
JSON مشوّه → 400 عام (الـ stack يظهر في لوق الخادم فقط، ليس في الاستجابة)؛ 2MB → 413؛ SQLi في المعرف (`' OR 1=1 --`) → 404 (استعلامات Prisma مُعلْلَمة)؛ CORS لا يمنح أصلاً مجهولاً؛ Helmet headers حاضرة؛ رفع HTML-as-PNG وSVG بامتداد png → **مرفوضان بفحص magic bytes**؛ XSS نصي يُخزَّن كنص حرفي (لا تنفيذ)؛ معاملات نطاق ناقصة → 400 مع رسالة عربية.

### المرحلة 8 — سجلات التدقيق (L01–L03): 3/3 ✅
رُصدت أثناء الهجوم: `TENANT_ACCESS_DENIED` (×8)، `LOGIN`، `CUSTOMER_ORDER_CREATED`، `PAYMENT_PROOF_SUBMITTED`، `BRANCH_CREATED`، `PROCESS_PAYMENT`، `STAFF_PIN_LOGIN`، `LOGOUT`. **لا توكنات/كلمات مرور في تفاصيل اللوق**، وmorgan يحجب `sessionToken`/`token`/`password` كـ `[REDACTED]` (مُشاهد في اللوق الحي).

### المرحلة 9 — مقاومة التخمين: ✅
- loginLimiter (20/15د لكل IP، skipSuccessful): تعطّل بـ **429** بعد المحاولات الفاشلة (أثبت نفسه عملياً مرتين — أثناء التشغيلات المتكررة وبفحص RL01 الموجه).
- pinLimiter (10/15د): 429 بعد **11** محاولة PIN خاطئة (فُحص بـ PIN غير موجود فعلاً؛ ملاحظة: 9999 في بذرة الاختبار هي PIN مدير A الحقيقي — ليست ثغرة).
- ملاحظة (SEC-04): العدادات في الذاكرة داخل العملية — فعّالة لنسخة واحدة فقط.

---

## النتائج (Findings)

### SEC-01 — بث الأحداث اللحظية (SSE) لا يوصل أي حدث للمتصفحات (gzip buffering)
| البند | التفصيل |
|---|---|
| **Severity** | **HIGH (P1)** |
| **Attack Surface** | `GET /api/public/events` (زوار + طاقم) |
| **Route/File** | `server/index.ts` (تركيب `compression()`) + `server/services/realtime.ts` (كتابة بدون `res.flush()`) |
| **Attack Scenario** | ليس هجوماً — عيب إتاحة: أي عميل متصفح يرسل `Accept-Encoding: gzip` (الافتراضي لكل المتصفحات وEventSource). |
| **Evidence** | موثق تجريبياً: فتح بث لطاولة A4، توليد نداء نادل (201 + بث WAITER_CALL للطاولة)، انتظار 5.5 ثانية → **0 بايت وصل** و`Content-Encoding: gzip`. نفس السيناريو مع `Accept-Encoding: identity` → حدث connected + WAITER_CALL فوراً. زليب يجمع الكتابات الصغيرة (لا flush لكل حدث)؛ دفقات القلب (8 بايت/25ث) لن تملأ المخزن إلا بعد ساعات. |
| **Impact** | تتبع طلب الزبون، شاشة المطبخ، نداءات النادل، وإشعارات POS — كل التحديثات اللحظية **صامتة الموت** للمتصفحات الحقيقية (تصل متأخرة بدفعات ضخمة أو عند إغلاق الاتصال). العزل لا يُخترق (البث يُرشَّح بالنطاق قبل الكتابة) لكن القناة معطوبة. |
| **Fix** | استثناء `text/event-stream` من الضغط: `app.use(compression({ filter: (req, res) => compression.filter(req, res) && res.getHeader('Content-Type') !== 'text/event-stream' }))` — أو استدعاء `res.flush()` بعد كل كتابة في realtime broadcast. (تعديل مقترح فقط — لم يُنفَّذ التزاماً بنطاق AUDIT ONLY). |
| **Release Blocker** | **YES** — للتحديث اللحظي؛ أو إعلان صريح أن الإصدار يعمل polling بدون تحديث لحظي. |

### SEC-02 — دليل النشر يتضمن قيم سرّية جاهزة للصق (JWT_SECRET وDB password حرفية)
| البند | التفصيل |
|---|---|
| **Severity** | MEDIUM (P2) |
| **Attack Surface** | `DEPLOYMENT_GUIDE.md` (متعقَّب في Git) — الأسطر 94 و97 |
| **Type / Evidence** | JWT_SECRET حرفية بطول 48 محرفاً hex (ليست نمط placeholder) + كلمة مرور DB حرفية 22 محرفاً لمستخدم محلي. أي منشئ يتبع الدليل حرفياً سينشر بإنتاجية **سر JWT معلَن publicly** → أي مهاجم يقدر على تزوير توكن PLATFORM_ADMIN لتلك النسخ. القيم غير مستخدمة في مستودع الكود نفسه (لا تطابق config الاختبار) — الخطر في "قابلية النسخ". |
| **Impact** | خطر تكويني لكل نشر يتبع الدليل دون توليد سر جديد. |
| **Fix** | استبدال القيم الحرفية بتعليمات توليد (`openssl rand -hex 32`) وقيمة placeholder واضحة `<GENERATE_ME>`؛ تدقيق أي نشر إنتاجي أُنشئ بهذا الدليل وتدوير السر. |
| **Release Blocker** | NO |

### SEC-03 — أسماء مضيفات قاعدة بيانات حقيقية في مستندات تدقيق متعقَّبة
| البند | التفصيل |
|---|---|
| **Severity** | LOW (P3) |
| **File** | `SECURITY_AUDIT_2026-09-09.md` (متعقَّب) |
| **Evidence** | عنوانا DB URL بنمط `aws-…pooler…:5432` (43 محرفاً) مع اسم مستخدم — **كلمات المرور محجوبة** في المستند. قيمة استطلاعية (hostname/username) فقط. |
| **Fix** | استبدال المضيف بـ `<redacted>` في المستندات المستقبلية؛ لا إجراء عاجل إن كانت البنية مهجورة/مستبدلة. |
| **Release Blocker** | NO |

### SEC-04 — عدادات Rate-limit وسجل SSE داخل الذاكرة (نسخة واحدة)
| البند | التفصيل |
|---|---|
| **Severity** | MEDIUM (P2 — مشروط بالتوسّع) |
| **File** | `server/middleware/rateLimit.ts`, `server/services/realtime.ts` |
| **Evidence** | loginLimiter/pinLimiter/SSE registry كلها في-process. موثّقة في الكود كقيد تصميمي. |
| **Impact** | عند التوسّع الأفقي (نسختان+): حدود التخمين تتضاعف لكل IP، والإبطال/البث لا يتقاطعان بين النسخ. النشر الحادي (Render service واحد) غير متأثر. |
| **Fix** | عند التوسع: Redis store لـ express-rate-limit + Pub/Sub للبث. يُدرج كقيد معماري معروف. |
| **Release Blocker** | NO (لنشر نسخة واحدة) |

### SEC-05 — التدقيق الأمني best-effort (لا يفشل الطلب عند فشل اللوق)
- **Severity**: LOW (P3) · **File**: `server/services/audit.ts` + مواضع `.catch(() => {})`.
- انقطاع DB لحظي قد يسقط أحداثاً أمنية بصمت. مقايضة متعمدة وموثقة (استمرارية الخدمة) — مقبولة، مع التوصية بمحاولة إعادة/عداد للسقطات.

### SEC-06 — سياسة مفاتيح PIN (4 أرقام) ضمن نطاق المستأجر
- **Severity**: LOW (P3) · **Attack Surface**: `POST /api/auth/pin`.
- مفاتيح 4 خانات (10⁴) مسموحة (البذرة تستخدمها). الحماية الفعلية: bcrypt + pinLimiter 10/15د/IP. الخطر المتبقي: هجوم موزَّع عبر عناوين IP متعددة (قصور inherit في الحدود لكل-IP دون CAPTCHA/قفل حسابي). **توصية**: رفع الحد الأدنى إلى 6 خانات أو إضافة قفل تصاعدي لكل حساب/مطعم.

### SEC-07 — جلسة الطاولة مشتركة بالتصميم
- **Severity**: INFORMATIONAL · **File**: `server/routes/public.ts` (سلوك موثق في الكود).
- زبونان يمسحان نفس QR يتشاركان جلسة ACTIVE واحدة ويرى كل منهما طلبات الآخر على الطاولة. قرار منتج مقصود (طاولة = جلسة)، ليس عزلاً بين مستأجرين. موثق هنا للشفافية.

---

## مصفوفة عزل المستأجرين (Tenant Isolation Matrix)
الاتجاهان A→B وB→A (توكنات صحيحة + تلاعب بكل المعرفات):

| المورد | قراءة (Read) | كتابة (Write) | حذف (Delete) |
|---|---|---|---|
| Restaurant | ✅ عام فقط (كتالوج)؛ إعدادات = نطاق التوكن | ✅ مقيد بنطاق التوكن | ✅ لا مسار حذف؛ الحالة = platform فقط |
| User/Staff | ✅ مقيّد بمستأجر التوكن | ✅ 403 (T18) + رفض ترقية الدور | ✅ 403 |
| Table | ✅ | ✅ 403 (T14) | ✅ مقيّد |
| QR | ✅ معتم؛ الكتالوج يخفي التوكنات | ✅ إنشاء الجلسة ملتزم بتوكن الطاولة | n/a (regenerate = نطاق المدير) |
| Category/Product | ✅ | ✅ 403 (T12) | ✅ 403 (T13) |
| Order | ✅ صفر بايتات B (T01) | ✅ 403/404 (T15) + idempotency | ✅ إلغاء الزائر ملتزم بالجلسة |
| Payment | ✅ (T06) | ✅ 409 مزدوج/404 (T20–T22) | ✅ void → 404 (T21) |
| Branch | ✅ (T07) | ✅ يهبط في مستأجر المُرسِل فقط (T25+DB) | ✅ |
| Analytics | ✅ صفر بايتات B (T09) | n/a | n/a |
| Audit Log | ✅ PLATFORM_ADMIN فقط | n/a | n/a |

**كل الخلايا: معزولة ✅ — لا اختراق في أي اتجاه أو عملية.**

## RBAC — ملخص الأدوار (متحقق ديناميكياً)
| الدور | عملياته المسموحة | مُنع من |
|---|---|---|
| PLATFORM_ADMIN | كل شيء عبر `/api/admin` (حارس على مستوى الـ router) | — |
| RESTAURANT_MANAGER | CRUD نطاقه، موظفون، مدفوعات، فروع | سجلات التدقيق، ترقية دوره، نطاق غيره |
| CASHIER | طلبات/مدفوعات نطاقه | الخطط، الإحصاءات، حذف المنتجات |
| WAITER/STAFF | طلبات نطاقه، طاولات، تسوية مقيدة | إدارة موظفين، خطط، فروع |
| KITCHEN | تغيير حالة الطلب (مسموح) | كل الإدارة |

## مسح الأسرار (نتائج فقط — القيم لم تُطبع قط)
| الفحص | النتيجة |
|---|---|
| `.env` الحقيقي متعقَّب في Git؟ | ❌ لا (`.gitignore` يغطي `.env*` باستثناء example) |
| Supabase service_role / API keys (sk-, ghp_, AKIA, xox) | ✅ غير موجودة في المتعقَّب |
| JWT_SECRET/DB password حرفية في متعقَّب | ⚠️ **SEC-02** (دليل النشر) + placeholder في `.env.example`/`docker-compose.yml` (سليم) |
| Hostnames بنية تحتية في مستندات | ⚠️ SEC-03 (منخفض) |
| تاريخ Git | المستودع استنساخ مضغوط (squash) — لا يمكن مسح التاريخ القديم هنا؛ يوجد gitleaks CI على push |

## من TEST 1 — F-04 (بوابة دفع الزبون)
مسار الواجهة الخلفية سليم تماماً في TEST 2: رفع إثبات الدفع لطلب الزبون نفسه → 201 + تسجيل PAYMENT_PROOF_SUBMITTED + بث SSE للطاقم (P08). خلل F-01 في TEST 1 كان في **عرض الواجهة** في live-preview — خارج نطاق هذا التقرير ويبقى معلَّقاً على بوابة تجربة المستخدم.

---

## ما تم التحقق منه كسليم (سجل إيجابي مختصر)
BCrypt (12 rounds) · JWT مع إعادة قراءة الدور/الحالة/النطاق من DB عند كل طلب حساس · إبطال tokenVersion · CSPRNG لكل التوكنات (QR/session) · تسعير خادمي صارم + idempotency بـ clientRequestId · CAS updates للحالات · reconcileCashPayment مع حماية مزدوجة · استعلامات Prisma مُعلْلَمة (لا SQLi) · فحص magic bytes للرفع + حد 5MB + نطاق مفتاح التخزين لكل مطعم · Helmet/CORS/حدود أحجام · رسائل خطأ عربية موحّدة بلا stack traces · حجب الرموز من لوقز الوصول والتدقيق.

---

```
SECURITY_GATE=CONDITIONAL_PASS
TENANT_ISOLATION=PASS
CRITICAL_VULNERABILITIES=0
HIGH_VULNERABILITIES=1 (SEC-01 SSE delivery outage — availability, not isolation)
NEXT_GATE=TEST_3
```
