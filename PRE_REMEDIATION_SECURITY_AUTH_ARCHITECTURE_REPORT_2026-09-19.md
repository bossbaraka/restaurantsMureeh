# PRE-REMEDIATION SECURITY & AUTHENTICATION ARCHITECTURE REPORT
**المشروع:** restaurantsMureeh (Mureeh Menu | مُريح) · **التاريخ:** 2026-09-19 · **الطبيعة:** تحليل فقط — **لم يُعدَّل أي سطر في المستودع**
**الأدوار:** Principal Software Architect / Principal Application Security Engineer / SaaS Security Architect / Senior Backend Engineer / Senior Frontend Engineer / QA & Test Architect / Database Architect / Restaurant SaaS Domain Analyst
**المدخلات:** تدقيق كود مباشر (كل اقتباس أدناه بملف:سطر) + الأدلة الديناميكية من TEST 1–4 (بما فيها 113/114 فحص أمني و104 فحص E2E).

---

## 1) Executive Summary

نظام المصادقة الحالي **أفضل بكثير من المتوسط لمشروع SaaS في هذه المرحلة**: JWT مثبَّت على HS256 مع issuer/audience، إبطال فوري عبر `tokenVersion`، إعادة تحقق من حالة الحساب والمطعم في **كل طلب**، مقارنات bcrypt مضبوطتة زمنياً، حراس تهيئة fail-closed، ولا حسابات تجريبية/أبواب خلفية. عزل المستأجرين 30/30 في TEST 2.

لكن تحليل هذا اليوم كشف **ثغرتين جديديتين من فئة P1 في مسار الموظفين** لم تكن مغطاة في TEST 1–4:

- **AUTH-01 (P1):** واجهة إضافة الموظف تُنشئ كلمة مرور للموظفين غير المديرين **مشتقة من الـ PIN نفسه** — `password: Staff-${generatedPin}!` (`StaffManagement.tsx:150`) مع بريد إلكتروني قابل للتخمين (`{name}@{slug}.com`). أي موظف (نادل/كاشير/مطبخ) يملك فعلياً سراً واحداً بقوة 10⁴ لا سرّين.
- **AUTH-02 (P1):** لا قفل حساب لكل مستخدم لمحاولات PIN الخاطئة — الحدود per-IP فقط وفي الذاكرة (`rateLimit.ts:44-53`). فضاء PIN رباعي الأرقام = 10,000 احتمال؛ تخمين موزَّع عبر عناوين متعددة يبقى ممكناً عملياً.

إضافة إلى ذلك: الجلسة **12 ساعة مطلقة بلا idle-timeout** وبلا refresh tokens على جهاز كاشير مشترك، الرمز في `localStorage`، الخروج يُبطل جلسات المستخدم على **كل** الأجهزة دفعة واحدة (لا جلسات لكل جهاز)، ولا يوجد مسار ذاتي لتغيير كلمة المرور (المدير وحده يعيد التعيين).

**الخلاصة المعمارية:** البنية الحالية = «هوية واحدة بسرّين منفصلين + JWT طويل بلا جلسات جانب-الخادم». خيار التحسين الأمثل ليس «كلمة مرور للجميع» ولا «PIN قوي للجميع» بل على الأرجح **نموذج هجين حسب الدور** (تحليل كامل بدون اختيار نهائي في §19): هوية قوية للمدير/المنصة، دخول PIN سريع للوردية التشغيلية مع ميزانية خطأ لكل حساب، جلسات قصيرة قابلة للتجديد منضبطة بجهاز، وstep-up للأفعال المالية/الحساسة — **قرار المالك مطلوب قبل أي تنفيذ.**

```
ANALYSIS_COMPLETE=YES · CODE_MODIFIED=NO · WAITING_FOR_OWNER_APPROVAL=YES
```

---

## 2) Current Authentication Architecture — الإعادة الكاملة للمسار الحالي

### 2.1 هوية الموظف (Identity)
`prisma/schema.prisma:181-206` — `RestaurantUser`:
| الحقل | الدور |
|---|---|
| `email` (فريد عالمياً، إلزامي) | معرّف الدخول الأساسي — **عالمي وليس لكل مستأجر** |
| `passwordHash` (إلزامي) | bcrypt cost **12** (`manager.ts:2521`) |
| `pinHash` (اختياري) | bcrypt cost **10** (`manager.ts:2524`) — لطاقم الوردية |
| `restaurantId` | المستأجر (Cascade delete) |
| `role` | `TenantRole` enum: `PLATFORM_ADMIN, SUPER_ADMIN, RESTAURANT_MANAGER, STAFF, WAITER, KITCHEN, CASHIER` (`schema.prisma:10-18`) |
| `status` | `ACTIVE/SUSPENDED/INACTIVE` — يُفحص في كل طلب |
| `tokenVersion` (`tv`) | عدّاد الإبطال — يُرفع عند الخروج/تغيير سر/دور/حالة |
| `lastLoginAt` | آخر دخول ناجح |

**لا يوجد:** جدول جلسات، أجهزة، refresh tokens، أسرار MFA، سجل محاولات دخول، WebAuthn credentials. الأدوار إلزامية بلا فروع (branch-scoped roles غير موجودة — `branchId` على الطاولات فقط).

### 2.2 مسارا الدخول (Backend)

**المسار 1 — بريد+كلمة مرور:** `POST /api/auth/login` (`server/routes/auth.ts:75-226`)
1. `loginLimiter`: 20 فشل/15 دقيقة/IP، يُحسب الفشل فقط (`rateLimit.ts:35-46`)
2. `loginSchema` (Zod): بريد ≤254 + كلمة مرور 1..128 (`schemas.ts:324-340`)
3. بحث بالمستخدم → `bcrypt.compare` ضد `DUMMY_HASH` إذا لم يوجد (تسطيح التوقيت — `auth.ts:32,104-107`)
4. **مفتاح تبديل الهوية:** لو أُرسل `pin` مع البريد/كلمة المرور ولم يطابق PIN صاحب الحساب → تُفحص PINs **كل موظفي المطعم النشطين** وتُحوَّل الهوية لمطابق الـ PIN (`auth.ts:113-134`) — آلية «المدير يفتح الجهاز ثم العامل يدخل برمزه»
5. فحص الحالة: موقوف → 403؛ مطعم موقوف → 403 (منصة معفاة)
6. `signToken` → JWT + تحديث `lastLoginAt` + AuditLog `LOGIN` بالـ IP

**المسار 2 — PIN طاقم:** `POST /api/auth/pin` (`auth.ts:295-415`)
1. `pinLimiter`: 10 فشل/15 دقيقة/IP (`rateLimit.ts:49-58`)
2. `pinLoginSchema`: `^\d{4,10}$` + `restaurantId` **إلزامي** (عزل المستأجر — `schemas.ts:342-349`)
3. جلب كل الموظفين النشطين بـ PIN في المطعم (باستثناء أدوار المنصة) → مقارنة bcrypt متوازية (`auth.ts:324-339`)
4. فشل → مقارنة DUMMY إضافية + 401 برسالة موحّدة (لا كشف أي مستخدم)
5. نجاح → JWT كامل + AuditLog `STAFF_PIN_LOGIN`

**الخروج:** `POST /api/auth/logout` → `tokenVersion: {increment: 1}` لكل مستخدم (`auth.ts:284`) — **يُبطل كل أجهزة المستخدم معاً**.

**استعادة كلمة المرور:** `POST /api/auth/password-reset-request` → **501 غير مفعّلة عمداً** بلا مزوّد بريد (`auth.ts:418-432`) — إعادة التعيين عبر المدير فقط (تحديث موظف).

### 2.3 الرمز (JWT) — بالضبط
`server/middleware/auth.ts:49-72` + `server/config.ts:25,252-254`:
- **الخوارزمية:** HS256 مثبّتة (رفض أي رمز بخوارزمية أخرى)
- **issuer:** `mureeh-api` · **audience:** `mureeh-app` — كلاهما مفروض عند التوقيع والتحقق
- **السر:** `JWT_SECRET` ≥32 حرفاً، **لا سر احتياطي أبداً** — boot يفشل بدونها (`config.ts:20-22`)
- **الصلاحية:** `JWT_EXPIRES_IN` افتراضي **12h**
- **Claims:** `{id, restaurantId, name, email, role, status, tv, jti}` — الـ `jti` يُولَّد (UUID) لكنه **لا يُخزَّن ولا يُفحص** (لا مكافحة replay عبر قاعدة — الحماية من إعادة الاستخدام تأتي من `tv`+الحالة فقط)
- **التخزين (الواجهة):** `localStorage` تحت `merar_auth_token` (`src/services/api.ts:98,607`) — **ليس** HttpOnly cookie؛ XSS في SPA = سرقة الرمز. لا Secure/SameSite (لا cookies أصلاً).
- **لا refresh tokens، لا تدوير، لا جلسات جانب-خادم.**

### 2.4 التحقق في كل طلب (القلب الأمني)
`authenticateToken` (`middleware/auth.ts:79-150`) — يعمل على **كل** مسار محمي:
1. فك الرمز مع `algorithms:[HS256]` + iss + aud
2. **استعلام DB طازج لكل طلب:** وجود المستخدم + `status=ACTIVE` + حالة المطعم `ACTIVE` (إيقاف مستأجر يقطع الوصول فوراً حتى برمز 12h صالح)
3. **مطابقة `tv`** مع `tokenVersion` الحالي — خرج/غيّر كلمة مرور/PIN/دور/حالة → الرمز القديم يموت في الطلب التالي
4. `requireAuth` / `requireRole(...)` / `requirePlatformAdmin` / `requireTenantAccess` (`middleware/auth.ts:153-235`): مدراء المنصة يتجاوزون فحص الدور (النطاق عبر-المستأجرين يُفحص صراحة في كل مسار)، والمستأجرون محصورون في `restaurantId` الخاص بهم

**نقاط رفع `tv`:** الخروج (`auth.ts:284`)، تحديث موظف مع تغيير كلمة مرور/PIN/دور/حالة (`manager.ts:2508-2524`).

### 2.5 الواجهة (Frontend) — المسار الكامل
تفاعل المستخدم → `LoginModal.tsx` (بطاقتان: بريد+كلمة مرور، أو لوحة أرقام PIN تعرض المطعم أو تطلب slug من رابط المنيو، `LoginModal.tsx:120-160`)
→ تحقق الواجهة (قفل محلي 5 محاولات/60 ثانية — **UX فقط**، `AuthContext.tsx:63-64`)
→ `api.login()/pinLogin()` → تخزين الرمز في localStorage (`api.ts:607,622`)
→ `AuthContext` يخزّن هوية المستخدم+المطعم في localStorage (`merar_user_session`, `merar_manager_restaurant`)
→ عند الإقلاع: `restoreSession()` يستدعي `/auth/me` ويتحقق من صلاحية الجلسة فعلياً (401 → مسح كامل) (`AuthContext.tsx:104-137`)
→ كل طلب لاحق يحمل `Authorization: Bearer` (`api.ts:541-548`)
→ 401/403 يعالَج **لكل شاشة على حدة — لا يوجد interceptor عام يفرض الخروج** (`api.ts:552-587` لا يفحص 401 مركزياً)
→ الخروج: `api.logout()` يُبطل الخادم أولاً ثم يمسح المحلي في `finally` (`api.ts:642-652`)

**حراس الواجهة:** `ROLE_VIEW_ACCESS` + `ROLE_MANAGER_TAB_ACCESS` (`AuthContext.tsx:26-56`) — مرآة (وليس بديلاً) لحراس الخادم؛ التعليق يوثّق أن CASHIER=POS/ORDERS/TABLES فقط، WAITER بلا POS، KITCHEN=ORDERS فقط.

**إدارة الموظفين (UI):** `StaffManagement.tsx` — إنشاء موظف بـ PIN مولَّد CSPRNG رباعي (1000-9999، `StaffManagement.tsx:139-141`)، دور المدير يتطلب كلمة مرور ≥8؛ **غير المدير يحصل على `Staff-${PIN}!`** (`:150`) وبريد تلقائي `{name}@{slug}.com` (`:143`). لا توجد واجهة «غيّر كلمتي» ذاتياً لأي دور.

### 2.6 البذور (Seed)
`e2e/seed-test-data.mjs:20,138-168` — PINs ثابتة (1111/2222/3333/4444/9999) وكلمة مرور موحدة للاختبار — **بيئة اختبار فقط**، ليست في إنتاج، ولا توجد حسابات default في كود الإنتاج (TEST 2 أثبتت إزالة حسابات demo نهائياً — C-01).

---

## 3) Current Threat Model — تهديدات عمليات المطعم

### فيزيائية (الواقع المطعمي)
| التهديد | الوضع الحالي |
|---|---|
| حاسوب كاشير مشترك | ⚠️ رمز 12h بلا idle-timeout — أول موظف يبقى «هو» حتى منتصف الوردية؛ التبديل يتطلب خروجاً يدوياً |
| جهاز نادل/تابلت مشترك | ✅ PIN-switch سريع (بريد مدير+PIN عامل) لكن ⚠️ يعتمد على بقاء جلسة المدير حية |
| شاشة مطبخ مشتركة | ✅ دخول PIN → KDS مباشرة؛ ⚠️ لا قفل تلقائي للشاشة |
| جهاز مفتوح تركه موظف | ❌ لا آلية idle-lock — أي شخص يكمل بصلاحيات صاحب الجلسة حتى 12h |
| جهاز مسروق | ⚠️ localStorage قابل للقراءة من الجهاز؛ الإبطال الوحيد: المدير يغيّر حالة/سر الحساب (يقتل كل أجهزة المستخدم) — لا إبطال جهاز بعينه |
| تجسس بصري على PIN | ⚠️ PIN 4 أرقام سهل الحفظ بالنظر؛ الواجهة تعرض النقاط ● (جيد) لكن لوحة الأرقام كبيرة وثابتة |
| مشاركة بيانات بين موظفين | ❌ لا يمكن منعها تقنياً بلا أجهزة موثوقة؛ الأثر محدود بصلاحيات الوردية |
| موظف سابق | ✅ إيقاف/تغيير دور يرفع `tv` → قطع فوري (ممتاز) — يعتمد على انتباه المدير |

### بيانات الاعتماد
| التهديد | الوضع |
|---|---|
| Brute-force PIN | ⚠️ **AUTH-02**: 10 محاولات/15د/**IP** فقط، بلا قفل لكل حساب؛ 10⁴ فضاء — توزيع عبر IPs يخترق |
| Brute-force كلمة مرور | ✅ معقول (20/15د/IP + bcrypt 12) لكن ⚠️ **AUTH-01**: كلمات مرور الموظفين مشتقة من PIN (فضاء 10⁴ فعلياً) وبريد قابل للتخمين |
| Credential stuffing | ⚠️ لا فحص Against قوائم تسريبات، لا سياسة تعقيد حقيقية (8+ وحظر قائمة شائعة فقط — `schemas.ts:73-81`) |
| سرقة الرمز (localStorage) | ❌ XSS = سرقة جلسة 12h كاملة؛ لا HttpOnly/صلاحية قصيرة |
| امتداد متصفح خبيث | ❌ نفس ما سبق (SPA وحدة واحدة تدير الرمز بنفسها) |
| الهندسة الاجتماعية | ⚠️ «مدير يعيد تعيين كلمة مرور أي موظف» بلا step-up — موظف ينتحل مديراً على شاشة مفتوحة يسيطر على كل الحسابات |
| انتحال موظف | ⚠️ PIN مشترك بين موظفين بنفس الرقم يلتقط أول مطابقة (`auth.ts:324-339` غير حتمي الترتيب) — لا قيد تفرد PIN لكل مستأجر |

### تخويل
| التهديد | الوضع |
|---|---|
| STAFF→CASHIER / WAITER→CASHIER | ✅ حراس خادمية صارمة (TEST 2: 15/15) — الواجهة ترشّح والخادم يرفض 403 |
| CASHIER→MANAGER | ✅ كذلك؛ إدارة الموظفين requireManager فقط |
| عبور مستأجر آخر | ✅ 30/30 في TEST 2؛ JWT tenant + requireTenantAccess + فحص ملكية الكيان في كل مسار |
| موظف موقوف | ✅ قطع في الطلب التالي (فحص status لكل طلب) |
| تغيير دور والجلسة حية | ✅ رفع `tv` فوري (`manager.ts:2508-2524`) |
| تجاوز قيود الفرع | ⚠️ **لا يوجد تمييز صلاحيات حسب الفرع أصلاً** — WAITER يرى طلبات كل فروع مطعمه (فجوة ميزة أكثر منها ثغرة) |

### تشغيلية
| التهديد | الوضع |
|---|---|
| انقطاع إنترنت | ✅ العميل يعيد المحاولة برسائل عربية نظيفة (TEST 4)؛ الخادم المحلي حي عند سقوط DB (TEST 4) |
| تبديل موظفين وقت الذروة | ✅ PIN-switch بثوانٍ — لكن كما سبق يعتمد على جلسة مدير حية |
| كاشير يحتاج جلسة مستمرة | ✅ 12h تكفي وردية كاملة — هذا بالضبط ملف الموازنة أمن/تشغيل |
| دخول طوارئ | ⚠️ لا يوجد «مفتاح طوارئ» موثّق — فقدان المدير الوحيد لكلمة مروره = تدخل PLATFORM_ADMIN عبر الخادم فقط (لا واجهة إعادة تعيين عبر المنصة مكتشفة في admin.ts عدا onboard) |

---

## 4) Current Security Controls — عناصر القوة (مُتحقَّق منها)

1. **HS256 مثبّت + iss/aud مفروضان** توقيعاً وتحقيقاً (`middleware/auth.ts:49-72,101-108`) — لا `alg:none`/التبديل مفاتيح.
2. **تحقق تفويض طازج لكل طلب** — حالة الحساب + حالة المستأجر + `tv` من DB في كل نداء محمي (`middleware/auth.ts:111-148`). إيقاف حساب/مستأجر يسري **فوراً** رغم رمز 12h.
3. **إبطال `tokenVersion`** على الخروج وتغيير سر/دور/حالة (`auth.ts:284`, `manager.ts:2508-2524`).
4. **تسطيح توقيت كامل** في المسارين (DUMMY_HASH — `auth.ts:32,104-107,338-341`) — مقاومة تعداد المستخدمين.
5. **bcrypt 12 (كلمات) / 10 (PINs)** — تكلفة كافية لفضاءاتها.
6. **حدود معدل خادمية** للدخول (20/15د) وPIN (10/15د) تحسب الفشل فقط (`rateLimit.ts:35-58`) — العميل lockout=5/60ث مجرد UX (موثّق بصراحة).
7. **PIN login معزول بالمستأجر إلزامياً** + استبعاد أدوار المنصة من PINs نهائياً (`auth.ts:295-320`).
8. **حماية آخر مدير** — لا خفض/إيقاف/حذف آخر RESTAURANT_MANAGER نشط (`manager.ts:2482-2500,2545+`).
9. **AuditLog بالـ IP** (خلف TRUST_PROXY) لنجاح الدخول/الخروج/تحديث الموظفين (`services/audit.ts`, `schema.prisma:556-572`).
10. **STAFF_SAFE_SELECT** يستبعد كل الهاشات من أي استجابة (`manager.ts:2366-2375`).
11. **لا حسابات demo ولا كلمات مرور حرفية في الكود** (أزيلت نهائياً — C-01 في TEST 2)؛ البذور الاختبارية معزولة في `e2e/`.
12. **Fail-closed config**: JWT_SECRET<32، CORS إنتاجي دقيق، storage production — كلها ترفض الإقلاع (TEST 4 تحقق عملياً).
13. **رسائل 401/403 عربية موحّدة** لا تكشف وجود الحساب أو دوره.

---

## 5) Current Weaknesses — نقاط الضعف (مرقّمة، جديدة اليوم ما لم يُشر لغيرها)

| ID | الخطورة | الوصف | الموقع الدقيق |
|---|---|---|---|
| **AUTH-01** | **P1** | كلمة مرور موظفي الوردية **مشتقة من الـ PIN**: `password: Staff-${generatedPin}!` مع بريد تلقائي قابل للتخمين `{name}@{slug}.com` — سرّان مستضعفان بدل واحد قوي؛ مسار الهجوم: تخمين `Staff-XXXX!` عبر مسار كلمة المرور (حدوده أخف: 20/15د/IP) | `src/components/manager/StaffManagement.tsx:139-150` + `server/routes/manager.ts` create-staff يقبل الكلمة كما هي |
| **AUTH-02** | **P1** | **لا قفل حساب لكل مستخدم** لمحاولات PIN/كلمة المرور الفاشلة — الحدود per-IP وفي الذاكرة؛ PIN رباعي = 10⁴؛ توزيع التخمين على IPs متعددة (موبايل/بروكسي) يبقى رخيصاً. (توسيع SEC-06 من TEST 2 التي صنّفته LOW منفرداً) | `server/middleware/rateLimit.ts:44-58` + غياب أي جدول محاولات/أحداث أمنية |
| **AUTH-03** | P2 | الخروج/تغيير السر يُبطل **كل أجهزة المستخدم معاً** — لا جلسات ولا أجهزة مسجلة؛ لا «خروج من هذا الجهاز فقط» ولا «خروج بعيد» لمدير عن جهاز مفقود | `tokenVersion` per-user (`auth.ts:284`) — تصميم إبطال خشن |
| **AUTH-04** | P2 | **12h مطلقة بلا idle-timeout ولا refresh tokens** على أجهزة مشتركة؛ الرمز في localStorage قابل للسرقة بXSS ويبقى صالحاً ساعات؛ لا تدرّج صلاحية (رمز مدير مسروق = كامل الصلاحيات 12h) | `config.ts:25` (12h) + `api.ts:607` (localStorage) + لا أي idle detection في `src/` |
| **AUTH-05** | P2 | **لا مسار ذاتي لتغيير كلمة المرور** لأي دور (لا endpoint) واستعادة البريد 501 — كل استرداد يمر عبر مدير المطعم؛ PLATFORM_ADMIN بدون واجهة إعادة تعيين لمدير مستأجر مفقود الحساب | `auth.ts:418-432` (501) + غياب change-password في كل `server/routes/` |
| AUTH-06 | P3 | **PIN-switch داخل login يطابق أول موظف يطابق PIN** — لا تفرد PIN لكل مستأجر؛ موظفان برقم واحد = دخول غير حتمي | `auth.ts:324-339` (find(Boolean) على ترتيب DB) |
| AUTH-07 | P3 | `jti` يُوقَّع ولا يُستخدم — بلا سجل إبطال لكل رمز لا فائدة عملية حالية (لكنه جاهز لمستقبل جلسات DB) | `middleware/auth.ts:64,68` |
| AUTH-08 | P3 | **محاولات الدخول الفاشلة غير مُدقَّقة** — AuditLog للنجاح فقط؛ لا أثر تحقيقي لتخمين مستمر | `auth.ts:194,286,386` فقط |
| AUTH-09 | معلومة | `restaurantId` للـ PIN-login يُجلب من slug **عام** — العزل هنا للتصحيح لا للسرية (تصميم مقبول، يوثَّق) | `LoginModal.tsx:120-127` |

---

## 6) Restaurant Operational Constraints — الواقع التشغيلي (بالأدلة)

**مدير المطعم (RESTAURANT_MANAGER):**
- دخول نادر: بريد+كلمة مرور، جهازه غالباً شبه شخصي/مكتبي؛ PIN مدير موجود اختيارياً (seed يضع 9999) لكنه غير ضروري
- **صلاحياته الحساسة** (كلها بلا step-up، جلسة 12h تكفي): إنشاء/تعديل/حذف/إيقاف موظفين وتغيير أدوارهم وإعادة تعيين كلمات مرورهم وPINs (`manager.ts:2395,2447,2545`)، العلامة التجارية والقوائم والعروض، **إلغاء مدفوعات** (`/payments/:id/void` — `manager.ts:4190`)، الاشتراك والفواتير، الفروع، تحليلات الإيرادات الكاملة، تصدير الطلبات، إعادة توليد QR الطاولات
- يمكنه تعديل سجلات مالية عبر void — **أعلى تركيز مخاطر مالية في المنصة**

**الكاشير (CASHIER):** جهاز مشترك شبه مؤكد (POS). يحتاج جلسة عمل مستمرة (وردية). عملياته: فتح طلبات، تسوية طاولات، قبض نقدي/بطاقة/محفظة/تقسيم، **void**، تأكيد/رفض تحويلات العملاء (`manager.ts:3577,3710,4190,4468` — requireCashierOrManager). التبديل السريع بين كاشيرين ضروري وقت الذروة. **أفعال تستحق step-up: void، تسوية مبلغ كبير.**

**النادل (WAITER):** هاتف مشترك/شخصي؛ دخول متكرر (كل وردية، وأحياناً عدة مرات). السرعة حرجة. صلاحياته: طلبات/طاولات/نداءات — لا POS ولا تحليلات (موثّق `AuthContext.tsx:38-47` ومطابق للخادم).

**المطبخ (KITCHEN):** شاشة KDS مشتركة معلّقة؛ دخول PIN → `setViewMode('KITCHEN_KDS')` مباشرة (`LoginModal.tsx:151-156`)؛ أقل الأدوار حساسية (حالات تحضير الطلب فقط).

**منصة المنصتين (PLATFORM_ADMIN/SUPER_ADMIN):** عبر-المستأجرين: إيقاف مستأجرين (`admin.ts:158`)، onboarding (`admin.ts:287`)، تبديل خطط. **يجب أن يتبعوا نموذجاً مختلفاً جذرياً** — أقل عدداً، أعلى صلاحية، لا PINs لهم أصلاً (`auth.ts:313-318` يستبعدهم)، ودخولهم عبر البريد+كلمة مرور فقط.

---

## 7) Authentication Models Analysis — تحليل النماذج (بلا اختيار)

### Model A — الوضع الحالي (بريد+كلمة مرور / PIN+مطعم)
- **قوة:** سرعة وردية ممتازة؛ إبطال فوري؛ عزل مستأجر مثبت 30/30؛ كلفة تشغيل صفر (لا بريد/أجهزة)
- **ضعف:** AUTH-01/02 (سر مشتق + بلا قفل حساب)؛ 12h بلا idle؛ إبطال خشن؛ لا استرداد ذاتي؛ الرمز في localStorage

### Model B — بريد/اسم مستخدم + كلمة مرور قوية فقط
- **أمن:** يقضي على فضاء 10⁴ ويوحد السر القوي؛ قابل لقفل حساب لكل مستخدم
- **تشغيل:** يكسر سرعة الوردية — نادل يكتب كلمة مرور معقدة على شاشة مشتركة كل تبديل = احتكاك حقيقي وقت الذروة؛ استعادة كلمة المرور تتطلب بريداً لكل موظف (غير واقعي لمطعم)؛ إدارة أرقام موظفين وفقرة عالية

### Model C — معرّف موظف + PIN فقط
- **أمن:** PIN 4 أرقام يبقى 10⁴ مهما طال؛ بلا قفل حساب لكل معرّف = قابل للتخمين (AUTH-02)؛ PIN 6 أرقام (10⁶) مع قفل حساب + تدقيق يحسّن كثيراً لكن يظل سراً قصيراً بطبيعته
- **تشغيل:** الأسرع على الإطلاق؛ لوحة أرقام واحدة؛ لا بريد مطلوب
- **ملاحظة حرجة:** الصلاحيات المالية للكاشير + PIN 4 أرقام مشترك الرؤية = توازن غير مريح؛ مقبول مع step-up وحدود لكل حساب

### Model D — كلمة مرور (هوية) + PIN قصير (تشغيل)
- **الفكرة:** الدخول الأول للجهاز بكلمة مرور قوية (هوية موثوقة) ثم كل تبديل/تأكيد حساس بـ PIN الشخصي
- **يحقق:** «هوية قوية + جلسة تشغيل سريعة + step-up» — المدير يفتح الجهاز، العاملون يتبادلون بـ PINs، وvoid/تغيير دور يتطلب PIN صاحب الصلاحية
- **الوضع الحالي يحتوي جذعاً من هذا النموذج** (login+PIN-switch، `auth.ts:113-134`) لكنه معطوب الأمان (AUTH-01: كلمة مرور الموظف مشتقة من PIN نفسه)
- **المطلوب لإتمامه:** فصل الأسرار فعلياً + قفل حساب لكل PIN + step-up middleware

### Model E — Passwordless / Passkeys / WebAuthn
- **دعم:** جيد في المتصفحات الحديثة، لكن أجهزة المطاعم (أندرويد اقتصادي/تابلت قديم/كروم) غير مضمونة؛ WebAuthn يرتبط بجهاز — **مطعم بأجهزة مشتركة = المفتاح المسجل على جهاز من ترك**
- **دوران الموظفين:** مرتفع في المطاعم؛ كل تعيين/مغادرة = تسجيل/إبطال credential — عبء إداري كبير على مدير غير تقني
- **الاسترداد:** فقدان الجهاز يحتاج مساراً إدارياً (مدير يعيد الإصدار) — ممكن لكنه عملية جديدة كاملة
- **الخلاصة:** ممتاز كـ**عامل ثانٍ للمدير/المنصة**، مكلف كآلية أساسية لطاقم الوردية

### Model F — MFA (TOTP / بريد OTP / SMS OTP / مفاتيح أمنية / passkeys)
- **TOTP:** الأنسب إن لزم — لا شبكة، تطبيق واحد، رخيص؛ إدارة استرداد (رموز backup) مطلوبة؛ عبء ضئيل لعدد محدود من المدراء
- **بريد OTP:** يتطلب بريداً لكل مستخدم + إرسالاً موثوقاً — المنصة **لا تملك مزوّد بريد أصلاً** (`auth.ts:418-432` — 501 مقصود)
- **SMS OTP:** **لا** — قابل للـ SIM-swap واعتراض SS7، وتكلفة، وشبكة المطاعم غير مستقرة أصلاً؛ لا يُفترض آمناً
- **مفاتيح أمنية/Passkeys:** أقوى عامل لمدير المنصة؛ نفس قيود Model E للطاقم
- **التوجيه التحليلي (لا قرار):** MFA منطقي لـ PLATFORM/SUPER_ADMIN وRESTAURANT_MANAGER (المفاتيح المالية)؛ غير منطقي لكل وردية

### Model G — هجين حسب الدور
- يبني على D: هوية قوية إلزامية للأدوار المالية/الإدارية، PIN+قفل حساب للوردية، جلسات قابلة للإدارة لكل جهاز، step-up للأفعال المالية — هو النموذج الوحيد الذي يعالج «جهاز مشترك + دوران موظفين + مخاطر مالية» معاً
- **كلفته:** أكبر نطاق تنفيذ (جلسات/أجهزة/محاولات دخول/step-up) — تفصيل في §13/§19

---

## 8) Role-Based Authentication Analysis — لكل دور على حدة (بلا ترتيب)

| البند | PLATFORM_ADMIN / SUPER_ADMIN | RESTAURANT_MANAGER | CASHIER | WAITER | KITCHEN / STAFF |
|---|---|---|---|---|---|
| المصادقة الأساسية الحالية | بريد+كلمة مرور (لا PIN مسموح) | بريد+كلمة مرور (+PIN اختياري) | PIN أو بريد+كلمة | PIN أو بريد+كلمة | PIN أو بريد+كلمة |
| MFA (تحليلي) | **موصى به بقوة** (TOTP/مفتاح) — أعلى صلاحيات المنصة | مبرر (void/موظفون/مالية) | غير عملي للوردية | لا | لا |
| مدة الجلسة الحالية | 12h | 12h | 12h | 12h | 12h |
| Idle timeout الحالي | لا يوجد | لا يوجد | لا يوجد (حاجة تشغيلية لجلسة طويلة) | لا يوجد | لا يوجد (شاشة معلّقة عمداً) |
| مهلة مطلقة مقترحة (تحليل) | قصيرة (1-4h) | متوسطة (8h وردية) | طويلة أو renew تلقائي (وردية) | وردية | طويلة (شاشة KDS) |
| إعادة مصادقة الأفعال الحساسة | غير موجودة | غير موجودة (void الموظفون بلا step-up) | غير موجودة (void بلا step-up) | — | — |
| Step-up تحليلي | كلمة مرور/MFA | كلمة مرور أو PIN + (MFA إن فُعّلت) | PIN | — | — |
| ثقة الجهاز الحالية | لا يوجد مفهوم | لا يوجد | لا يوجد | لا يوجد | لا يوجد |
| جلسات متزامنة حالياً | غير محدودة (كل رمز مستقل حتى إبطال tv) | كذلك | كذلك | كذلك | كذلك |
| خروج قسري (مدير يطرد جلسة موظف) | ❌ غير ممكن إلا بتغيير سر/حالة (يقتل كل أجهزة المستخدم) | ❌ كذلك | — | — | — |
| سلوك تغيير الدور | ✅ يبطل فوراً (tv) | ✅ | ✅ | ✅ | ✅ |
| سلوك إيقاف الحساب | ✅ يقطع في الطلب التالي | ✅ | ✅ | ✅ | ✅ |
| الاسترداد | DB/منصة فقط | عبر PLATFORM_ADMIN (لا واجهة مكتشفة) | مدير يعيد تعيين PIN/كلمة | كذلك | كذلك |

---

## 9) Session Security Analysis — الجلسات الحالية مقابل التهديدات

| التهديد | محمي؟ | الآلية/الفجوة |
|---|---|---|
| Session fixation | ✅ (غير قابل للتطبيق) | لا cookies — الرمز يصدر جديداً كل دخول؛ لا session id يُعاد استخدامه |
| سرقة الرمز (XSS/امتداد) | ❌ | localStorage + 12h صلاحية بلا تدوير |
| Token replay | ⚠️ جزئياً | الرمز المسروق يعمل من أي مكان حتى إبطال tv (الطلب الشرعي التالي للـ tv يكشف؟ لا — tv يُرفع فقط عند أحداث محددة)؛ jti غير مخزّن |
| جلسات قديمة (stale) | ✅ | فحص حالة DB لكل طلب يقطع الموقوفين فوراً |
| جلسات متزامنة | ⚠️ | غير محدودة ولا مرئية — لا يمكن للمدير رؤية «من متصل الآن» |
| خروج بلا إبطال | ✅ | logout يرفع tv خادمياً قبل مسح المحلي (`api.ts:642-652`) |
| تغيير كلمة مرور والجلسة القديمة حية | ✅ | credentialsChanged → tv++ (`manager.ts:2508-2524`) |
| تغيير دور والـ JWT القديم حي | ✅ | نفس الآلية |
| إيقاف موظف وجلسته حية | ✅ | فحص status لكل طلب |
| استمرار المتصفح (persistence) | ⚠️ بالتصميم | localStorage يعيش إغلاق التبويب — مقصود لجهاز كاشير، خطر على جهاز شخصي |
| Refresh token theft/reuse | غير مطبّق | لا refresh tokens أصلاً — أي إضافة مستقبلية تحتاج تدويراً + كشف إعادة استخدام |
| اختطاف جلسة على الشبكة | ✅ (نقلاً) | HTTPS إلزامي في الإنتاج (Render) + لا سر في URL |

**هل يجب الانتقال مستقبلاً إلى:** رموز وصول قصيرة + refresh بتدوير + سجلات جلسات/أجهزة + token versioning لكل جلسة + إبطال عند الأحداث الأمنية؟ — **التحليل يميل إلى نعم للأدوار المالية** (يكشف سرقة الرمز خلال دقائق بدل ساعات، ويمكّن «خروج هذا الجهاز») مع **استثناء تشغيلي متعمد لشاشات KDS/POS** (جلسة جهاز طويلة بلا تفاعل). هذا خيار معماري، لا قرار — §19.

---

## 10) Device Security Analysis — الأجهزة في واقع المطعم

النموذج الشخصي (كلمة مروري+MFA لكل فرد على جهازه) **لا يناسب** أجهزة مطعم مشتركة، وMureeh لا يملك اليوم أي مفهوم جهاز. الكيانات المحتملة (تحليل، لا تنفيذ):
- **جهاز موثوق مسمّى** (POS-1, KDS-مطبخ, تابلت-الصالة): تسجيل بلمسة موافقة من مدير؛ جهاز الكاشير يحمل «جلسة جهاز» طويلة، والعامل يتبدل فوقها بـ PIN — يحل تناقض «جلسة مستمرة + هوية متغيرة»
- **إدارة جلسات نشطة + خروج بعيد + إبطال جهاز**: يعالج الجهاز المسروق/المفقود (AUTH-03) بدون قتل كل أجهزة المستخدم
- **صلاحيات حسب الجهاز** (POS فقط يجري المدفوعات، KDS لا): يرفع العزل لكنه تعقيد إضافي — قد يكون over-engineening لمرحلة ما بعد الإطلاق
- **نطاق الحاجة الفعلي (تحليلي):** أجهزة الوردية (كاشير/مطبخ) تستفيد فعلاً؛ أجهزة النُدل والهواتف الشخصية تكفيها هوية المستخدم بلا تسجيل جهاز

---

## 11) Sensitive Action Security — الأفعال التي تستحق step-up (موجودة اليوم بلا أي تأكيد)

| الفعل | المسار | من يستطيع اليوم | تحليل step-up |
|---|---|---|---|
| إلغاء دفع (void) | `manager.ts:4190` | CASHIER وMANAGER | **نعم** — أثر مالي مباشر؛ PIN الكاشير/كلمة مرور المدير |
| تعديل كلمة مرور/PIN/دور موظف | `manager.ts:2447` | MANAGER | **نعم** — إعادة إدخال كلمة مرور المدير (درع ضد شاشة مدير متروكة) |
| إيقاف/حذف موظف | `manager.ts:2447,2545` | MANAGER | **نعم** — كلمة مرور |
| تغيير خطة/اشتراك مستأجر | `admin.ts` + `manager.ts:2838` | MANAGER/PLATFORM | **نعم** — MFA للمنصة |
| إيقاف مستأجر | `admin.ts:158` | PLATFORM | **نعم** — MFA |
| تأكيد/رفض دفع تحويل عميل | `manager.ts:4468` | CASHIER/MANAGER | ربما — PIN |
| تسوية طاولة | `manager.ts:3710` | CASHIER/MANAGER | لا (عمل روتيني عالي التكرار — step-up هنا يكسر الذروة) |
| فتح طلب/قبض نقدي | POS | CASHIER | لا — روتين وردية |
| تغيير Branding/إعدادات المطعم | `manager.ts` | MANAGER | اختياري — كلمة مرور |
| تغيير ملكية المطعم | غير موجود كمسار | — | عند إضافته: step-up قوي إلزامي |

**الخلاصة التحليلية:** الحد الأدنى عالي القيمة = void + إدارة موظفين + الاشتراك. اليوم أي جلسة كاشير/مدير مفتوحة تؤديها بلا فرق.

---

## 12) Security vs UX Matrix — المصفوفة المطلوبة (بلا فائز)

| نموذج المصادقة | Security | Speed | Shared Device | Recovery | Complexity | Restaurant Fit |
|---|---|---|---|---|---|---|
| A — الحالي (بريد+كلمة / PIN4) | **Weak-to-Moderate** (AUTH-01/02، إبطال ممتاز) | **Strong** (ثوانٍ) | **Strong** (PIN-switch) | **Weak** (مدير فقط، 501) | **Low** (مطبق) | Moderate — يعمل لكن بثقوب |
| B — كلمة مرور قوية فقط | **Strong** | **Weak** (كتابة طويلة بكل تبديل) | **Weak** | Weak-Moderate (يتطلب بريداً للجميع) | Medium | **Weak** لطاقم الوردية؛ قوي للمدير |
| C — معرّف+PIN فقط | **Weak** (10⁴–10⁶، قابل للتخمين بلا قفل حساب) | **Strong+** | Strong | Moderate (مدير يعيد الإصدار) | **Low** | Strong للوردية؛ Weak للكاشير المالي |
| D — كلمة مرور + PIN تشغيلي | **Strong** (فصل الهوية عن الوردية + step-up) | **Strong** (PIN بعد فتح الجهاز) | **Strong** (تبديل بـ PIN) | Moderate (استرداد المدير للـ PIN) | Medium-High | **Strong** — يعكس واقع المطعم |
| E — Passkeys/WebAuthn | **Strong+** (عامل واحد قوي مقاوم للتصيد) | Moderate (لمسة/biometric) | **Weak** (مرتبط بالجهاز؛ دوران الموظفين) | **Weak** (فقدان جهاز = مسار إداري) | High | Weak للوردية؛ Strong للمدير/المنصة |
| F — MFA (TOTP/مفتاح) | **Strong+** كعامل إضافي | Weak كخطوة وردية | Moderate | Weak-Moderate (رموز backup) | Medium-High | قوي للمدير/المنصة فقط؛ ضار إذا فُرض على الكاشير |
| G — هجين حسب الدور (D+E/F انتقائياً) | **Strong** | Strong (لكل دور ما يناسبه) | Strong | Moderate | **High** (أكبر نطاق) | Strong — إن كُلّف نطاق منضبط |

---

## 13) Database Impact — الكيانات التي قد يتطلبها التصميم (تحليل فقط — لا هجرة ولا Prisma)

| الكيان المقترح | لماذا قد يلزم | حقول مفتاحية | ملكية المستأجر | اعتبارات |
|---|---|---|---|---|
| `AuthSession` (جلسات جانب-خادم) | «خروج هذا الجهاز»، رؤية الجلسات النشطة، idle-timeout حقيقي، إبطال انتقائي (AUTH-03) | id, userId, deviceLabel, refreshHash, idleExpiresAt, absoluteExpiresAt, revokedAt, lastSeenAt, ip, userAgent | عبر userId→مطعم | استبقاء: حذف المنتهية بعد 30 يوماً؛ كل JWT يُفحص ضد الجلسة (كلفة DB لكل طلب — نفس كلفة فحص tv الحالي) |
| `RefreshToken` (إن فُصل عن الجلسة) | تدوير + كشف إعادة استخدام | tokenHash, sessionFK, rotatedFrom, expiresAt, usedAt | عبر الجلسة | تخزين **هاش** فقط؛ إعادة استخدام توكن مُدوَّر = حدث أمني يبطل السلسلة |
| `Device` (جهاز موثوق) | جلسة جهاز كاشير/KDS طويلة الأمد منفصلة عن هوية العامل | id, restaurantId, name, type, enrolledBy, lastSeenAt, revokedAt | **للمطعم مباشرة** | توازن البساطة: قد يُدمج في AuthSession كتسمية فقط |
| `MfaSecret` / `WebAuthnCredential` | MFA للمدير/المنصة (Model F/E) | userId, type(totp/webauthn), secretHash/credentialId, confirmedAt, recoveryCodesHash | عبر userId | كثرة مستخدميها قليلة (مدراء) — كلفة تشغيل ضئيلة |
| `RecoveryCode` | فقدان MFA/passkey | userId, codeHash, usedAt | عبر userId | إظهار مرة واحدة؛ 10 رموز لكل تفعيل |
| `LoginAttempt` / `SecurityEvent` | قفل حساب لكل مستخدم (AUTH-02) + تدقيق الفشل (AUTH-08) | identifier(email/userId), restaurantId, ip, outcome, at | عبر userId/مطعم | استبقاء قصير (30-90 يوماً) + فهارس على (identifier, at) — وإلا تنفجر الجدول بالتخمين المستمر |
| `AuthChallenge` | step-up مؤقت (void/إدارة موظفين) | userId, purpose, challengeHash, expiresAt (دقائق), consumedAt | عبر userId | عناصر قصيرة العمر؛ تنظيف دوري |

**أثر على المخطط الحالي:** `RestaurantUser` يظل كما هو (قد يضاف حقل `mfaEnabled`/`pinLength` لاحقاً)؛ `tokenVersion` يبقى كطبقة إبطال حتى لو أضيفت الجلسات (توافق خلفي كامل للرموز القائمة).

---

## 14) Migration Strategy — كيف ينتقل المستخدمون (تحليل)

1. **الموظفون الحاليون وPINsهم:** تبقى صالحة — bcrypt موجود؛ أي نموذج جديد يقرأ نفس `pinHash`. الإلزامي: **إصدار كلمات مرور حقيقية** لحسابات الوردية بدل `Staff-{PIN}!` (AUTH-01) — إما تعطيل مسار كلمة المرور للموظفين نهائياً (يُدخلون بـ PIN فقط) أو إعادة تعيين جماعية عند أول دخول.
2. **المديرون:** بلا تغيير إلزامي؛ تفعيل MFA = flow تفعيل اختياري ثم إلزامي لاحقاً (تدرّج).
3. **الرموز القائمة عند التقديم:** **السؤال الحاسم** — الجواب بالحالة الحالية: كل رمز يحمل `tv`؛ أول فحص بعد النشر يعيد التحقق من DB. إذا أضيفت جلسات جانب-خادم ولم تُهاجر، كل رمز قديم = «جلسة غير معروفة» → يجب قرار: (أ) فترة سماح تعامل القديم كجلسة ضمنية حتى انتهاء 12h، أو (ب) رفع tv لكل المستخدمين = خروج الجميع مرة واحدة (مقبول قبل الإطلاق؛ غير مقبول بعده). **الخيار (أ) أنعم ويكفي قبل الإطلاق لأن عدد الجلسات الحالية إنتاجياً = صفر تقريباً.**
4. **البذور/البيانات الاختبارية:** معزولة في `e2e/` — تُحدَّث مع أي تغيير عقد فقط.
5. **التراجع (rollback):** أي مرحلة تُنفذ كـfeature-flag خادمي (مثل STORAGE_DRIVER) — إعادة النشر بالإعداد القديم تعمل لأن الحقول القديمة لا تُحذف أبداً في نفس الهجرة.
6. **طوارئ المدير الوحيد المفقود:** يحتاج مساراً موثقاً (PLATFORM_ADMIN يعين كلمة مرور عبر واجهة/سكربت مراجَع) — اليوم غير موجود ولوثّق.
7. **إيقاف/تغيير أدوار أثناء الهجرة:** يعمل اليوم (tv) ويجب ألا يتراجع — أي نموذج جلسات يجب أن يبطل عند نفس الأحداث (security-event invalidation).

---

## 15) Recovery & Failure Modes — التعافي بلا باب خلفي

| السيناريو | الحالي | المتطلب التحليلي |
|---|---|---|
| نسيان موظف PIN | مدير يعيد تعيينه (يعمل ✅) | يكفي + AuditLog (موجود) |
| نسيان مدير كلمة المرور | PLATFORM_ADMIN (بلا واجهة مكتشفة) ⚠️ | مسار موثّق بإعادة تعيين مُدقَّقة |
| فقدان جهاز | قتل كل جلسات المستخدم (خشن) ⚠️ | إبطال جهاز بعينه (يحتاج كيان جلسات/أجهزة) |
| مغادرة موظف | إيقاف = قطع فوري ✅ | يكفي |
| فقدان جهاز MFA | غير مطبق | رموز Backup + مسار إداري مُدقَّق — **لا** رمز طوارئ ثابت |
| فقدان passkey | غير مطبق | كذلك |
| لا إنترنت | الرسائل عربية نظيفة ✅ (TEST 4) | أي MFA يجب أن يكون **offline** (TOTP) لا OTP شبكي |
| قاعدة بيانات معطلة | 500 عربي، خادم حي ✅ (TEST 4) | الدخول مستحيل بطبيعته (bcrypt في DB) — مقبول وموثّق |
| جلسة منتهية | شاشة لكل عرض على حدة ⚠️ | interceptor 401 موحد يعيد للدخول بلطف |
| حساب مقفول (تخمين) | غير موجود (AUTH-02) | قفل مؤقت متناسب + فك بيد المدير فقط |
| حساب مخترق | مدير: تغيير كلمة+tv يقطع ✅ | + إنهاء كل الجلسات وأحداث أمنية |
| طوارئ إدارية كاملة | DB مباشرة | سكربت مُدقَّق موثّق (break-glass) — يُسجَّل ويُراجع |

**القاعدة:** كل مسار تعافٍ يمر عبر مدير/منصة مُدقَّقة — لا استعادة ذاتية بلا هوية مثبتة (بلا بريد فعلي أصلاً، وOTP عبر SMS مستبعد أمنياً).

---

## 16) Security Test Strategy — خطة الاختبار المستقبلية (تصميم فقط)

**وحدات (unit):** سياسات كلمة المرور/PIN؛ منطق القفل لكل حساب؛ انتهاء idle/absolute؛ صحة تدوير refresh؛ توليد رموز الاسترداد.
**تكامل (API، DB حقيقية كما في F-02 المُصلحة):** دخول صحيح/خاطئ؛ تعداد مستخدمين (توقيت موحّد)؛ تخمين موزَّع (عدة IPs) ضد قفل الحساب؛ تبديل هوية بـ PIN؛ tv عند كل حدث؛ عزل مستأجر في PIN-login.
**تصريح:** مصفوفة أدوار×مسارات (استمرار اختبارات TEST 2 R01-R15)؛ صلاحية عبر-مستأجرين؛ موقوف/مغيَّر دوره والجلسة حية.
**جلسات:** انتهاء idle/absolute؛ خروج جهاز واحد vs الكل؛ جلسات متزامنة مرئية؛ إعادة استخدام refresh مُدوَّر = إبطال سلسلة؛ replay برمز ملغى.
**step-up:** طلب void/إدارة موظفين بلا تحدي → رفض؛ تحدي منتهي؛ تحدي مستهلك؛ صلاحية تحدي دور خاطئ.
**تعافٍ:** إعادة تعيين PIN/كلمة بيد المدير؛ رموز backup صالحة مرة؛ محاولات takeover.
**E2E متصفح:** رحلة كاشير كاملة مع تبديل عاملين على جهاز واحد؛ KDS بجلسة جهاز؛ مدير+MFA.
**انحدار أمني:** إعادة حزمة TEST 2 كاملة (113 فحصاً) بعد كل تغيير — **شرط بوابة**.

---

## 17) TEST 1–4 Master Findings Matrix — الدمج الكامل (SSE يُحسب مرة واحدة)

| ID | النتيجة | المصدر | التصنيف | الخطورة | الحالة | الإجراء المقترح |
|---|---|---|---|---|---|---|
| MF-01 | بث SSE لا يوصل للمتصفحات مع gzip (0 بايت) | SEC-01 (T2) = P1-B (T3) = تأكيد T4 | **عيب منتج** | **P1** | مفتوح (T4 أعاد إثباته على بناء 2026-09-19) | إصلاح compression/buffering في طبقة SSE |
| MF-02 | Walk-in/Counter POS → 500 (FK Order_tableId_fkey) | P1-A (T3) = تأكيد T4 | عيب منتج | **P1** | مفتوح | إصلاح manager.ts:705 أو حظر الخيار |
| MF-03 | Rules-of-hooks في CustomerLayout.tsx:328,332 → lint exit 1 | F-01 (T1) = تأكيد T4 | عيب منتج/جودة | **P1** | مفتوح | إعادة هيكلة الشروط |
| MF-04 | CI يشغّل gitleaks فقط — لا build/lint/test | F-03 (T1) | بنية تحتية | **P1** | مفتوح (تحقق اليوم: `.github/workflows/secret-scan.yml` وحيداً) | بوابة CI كاملة |
| MF-05 | 9 اختبارات DB-gated معطوبة (body.id، محلّل تعليقات SQL) | F-02 (T1) = تأكيد T4 | **عيب اختبارات** (تغطية) | **P1** | مفتوح | إصلاح الاختبارات وتشغيلها في CI |
| **MF-06** | **كلمة مرور موظفي الوردية مشتقة من الـ PIN + بريد قابل للتخمين** | **جديد — تحليل اليوم (AUTH-01)** | **ثغرة أمنية** | **P1** | مفتوح | فصل الأسرار / تعطيل مسار كلمة المرور للوردية |
| **MF-07** | **لا قفل حساب لكل مستخدم ضد تخمين PIN/كلمة المرور (حدود per-IP فقط)** | جديد اليوم (AUTH-02) ⊃ SEC-06 (T2: LOW) | ثغرة أمنية | **P1** | مفتوح | LoginAttempt + قفل تدريجي |
| MF-08 | F-04: حاضنة UI دفع الزبون في live-preview تفشل | T1 → فرز T2/T3 (API يعمل P08) | عيب حاضنة اختبار (أثر منتج غير محسوم) | P2 | مفتوح | فرز بصري ثم إصلاح |
| MF-09 | docker-compose لا يقلع في وضع production (حارس التخزين) | F-07 (T1) — تحقق اليوم (NODE_ENV: production بلا STORAGE_*) | بنية تحتية | P2 | مفتوح | ضبط compose |
| MF-10 | لا SIGTERM/إيقاف رشيق | F-08 (T1) | عيب خادم | P2 | مفتوح | معالجات إيقاف |
| MF-11 | Float للقيم النقدية | F-09 (T1) | دَين معمارية | P2 | مفتوح | Decimal في مرحلة لاحقة |
| MF-12 | strict:false + 140 تشخيص خادم غير موصولة | F-10 (T1) = تأكيد T4 | دَين جودة | P2 | مفتوح | ربط البوابات |
| MF-13 | اختبارات نص-ثابت تدّعي تغطية | F-11 (T1) | دَين اختبارات | P2 | مفتوح | تحويل لسلوكية |
| MF-14 | حاضنات guard تحتاج deps خارج devDependencies | F-12 (T1) | دَين اختبارات | P2 | مفتوح | إضافة devDeps |
| MF-15 | حدود معدل وسجل SSE داخل الذاكرة (نسخة واحدة) | F-13 (T1) = SEC-04 (T2) | بنية تحتية | P2 | مفتوح (موثّق) | Redis عند التوسع |
| MF-16 | أسرار جاهزة للصق في دليل النشر | SEC-02 (T2) | توثيق أمني | P2 | مفتوح | تعقيم المستند |
| MF-17 | أسماء مضيفات DB في مستندات متعقَّبة | SEC-03 (T2) | توثيق أمني | P3 | مفتوح | تعقيم |
| **MF-18** | **الخروج/تغيير السر يقتل كل أجهزة المستخدم؛ لا جلسات لكل جهاز ولا خروج بعيد** | جديد اليوم (AUTH-03) | دَين معمارية أمنية | P2 | مفتوح | كيان جلسات |
| **MF-19** | **12h مطلقة بلا idle-timeout/refresh + الرمز في localStorage** | جديد اليوم (AUTH-04) | دَين معمارية أمنية | P2 | مفتوح | جلسات/رموز قصيرة (حسب الدور) |
| **MF-20** | **لا مسار ذاتي لتغيير كلمة المرور؛ الاستعادة 501** | جديد اليوم (AUTH-05) | فجوة ميزة/أمن | P2 | مفتوح | endpoint تغيير ذاتي + مسار مدير |
| **MF-21** | رسالة 503 تقول «قاعدة البيانات» لكل فشل شبكة | T4 | عيب UX تشخيصي | P3 | مفتوح | صياغة |
| MF-22 | حزمة واحدة 998.5kB بلا تقسيم | F-14 (T1) = تأكيد T4 | أداء | P3 | مفتوح | code-splitting |
| MF-23 | deps ميتة + أوصاف README قديمة | F-15 (T1) | دَين | P3 | مفتوح | تنظيف |
| MF-24 | Dockerfile ينسخ devDeps | F-16 (T1) | دَين | P3 | مفتوح | متعدد المراحل |
| MF-25 | prisma:error ضجيج تصادم #N | F-17 (T1) | تشغيل | P3 | مفتوح | مستوى لوق |
| MF-26 | AuditLog best-effort + الفشل غير مُدقَّق | SEC-05 (T2) = AUTH-08 اليوم | دَين أمني | P3 | مفتوح | تسجيل الفشل |
| MF-27 | جلسة طاولة مشتركة بالتصميم | SEC-07 (T2) | توثيق | P3 | موثّق | بقاء التوثيق |
| **MF-28** | PIN-switch يلتقط أول مطابق (لا تفرد PIN/مستأجر) | جديد اليوم (AUTH-06) | عيب تصميمي صغير | P3 | مفتوح | قيد تفرد أو رسالة غموض |
| **MF-29** | صور /uploads بلا cache (max-age=0) | T4 | أداء | P3 | مفتوح | ترويسة تخزين |
| **MF-30** | /api/health لا يفحص DB (عمى مراقبة) | T4 | تشغيل | P3 | مفتوح | فحص خفيف |

**ملاحظة الدمج المطلوبة:** MF-01 = SEC-01 (T2) + P1-B (T3) + تأكيد T4 — **ثلاثة تقارير، عيب واحد.** كذلك MF-03/04/05/12/15/22 كلٌّ منها أُكد عبر اختبارين فدمج.

---

## 18) Consolidated Remediation Backlog — القائمة الموحدة (مُتحقَّق كل بند ضد المستودع اليوم)

### P1 — عوائق إطلاق / حرجة (7)
1. **MF-01** إصلاح SSE+gzip (عيب منتج — realtime ميت للمتصفحات)
2. **MF-02** إصلاح Walk-in POS 500 أو حظر الخيار صراحة (قرار منتج)
3. **MF-03** إصلاح خطأي rules-of-hooks → lint أخضر
4. **MF-04** CI: build+lint+test في كل PR
5. **MF-05** إصلاح الاختبارات التسعة المعطوبة وتشغيلها فعلياً
6. **MF-06 (AUTH-01)** فصل كلمة مرور الوردية عن الـ PIN (أو قصر دخول الوردية على PIN وتعطيل كلمة المرور لحسابها)
7. **MF-07 (AUTH-02)** قفل حساب تدريجي لكل مستخدم + تدقيق المحاولات الفاشلة

### P2 — مهم (14)
MF-08 فرز F-04 بصرياً · MF-09 docker-compose · MF-10 إيقاف رشيق · MF-11 Decimal للمالية · MF-12 strict + بوابات الأنواع · MF-13 اختبارات سلوكية · MF-14 devDeps للحاضنات · MF-15 مخزن حدود موزّع عند التوسع · MF-16 تعقيم أسرار الدليل · MF-18 جلسات/أجهزة (مع التصميم المعتمد) · MF-19 idle/refresh حسب الدور · MF-20 تغيير كلمة مرور ذاتي · MF-21 صياغة 503 · **+ أثر بوابة الأمن للتحقق من عدم انحدار SEC بعد إصلاحات P1**.

### P3 — بعد الإطلاق (11)
MF-17 تعقيم مستندات · MF-22 تقسيم الحزمة · MF-23 تنظيف deps/README · MF-24 Dockerfile · MF-25 ضجيج اللوق · MF-26 تدقيق الفشل · MF-27 توثيق جلسة الطاولة · MF-28 تفرد PIN · MF-29 cache الصور · MF-30 health مع DB · AUTH-07 توثيق jti.

---

## 19) Authentication Architecture Options — الخيارات المعروضة للمالك (2–4، بلا اختيار)

> سبعة نماذج حُللت في §7؛ المعروضة هنا هي التركيبات القابلة للتنفيذ فعلياً على هذا الكود.

### الخيار 1 — «تصليب النموذج الحالي» (أصغر نطاق)
- **المعمارية:** يبقى كل شيء كما هو + : كلمات مرور حقيقية للوردية (قتل `Staff-{PIN}!`) أو قصر الوردية على PIN فقط؛ `LoginAttempt` + قفل تدريجي لكل حساب؛ PIN ≥6 اختيارياً؛ step-up خفيف بـ PIN للأفعال المالية (challenge قصير في نفس JWT).
- **UX:** لا شيء يتغير للموظفين؛ المدير لا شيء.
- **أمن:** يغلق AUTH-01/02 وstep-up المالي؛ يبقي 12h/localStorage/الإبطال الخشن.
- **الوردية:** كما اليوم — ممتاز.
- **الأجهزة المشتركة:** كما اليوم (PIN-switch).
- **التعافي:** كما اليوم + قفل قابل للفك بيد المدير.
- **الهجرة:** إصدار كلمات مرور جديدة لموظفين قائمين عند أول دخول؛ لا تغيير JWT إطلاقاً.
- **النطاق:** صغير (backend: 2 endpoints + جدول؛ frontend: نُدل إدخال step-up).
- **مخاطرة جديدة:** تكديس patch على تصميم بلا جلسات — الديون المعمارية (MF-18/19) تبقى.

### الخيار 2 — «هوية قوية + PIN وردية + step-up» (Model D كاملاً)
- **المعمارية:** المدير/المنصة: كلمة مرور قوية إلزامية (+MFA اختياري للمنصة). الوردية: PIN ≥6 مع قفل لكل حساب؛ PIN فريد لكل مستأجر. الأجهزة المشتركة: جلسة «فتح جهاز» بمدير/مسؤول وردية ثم تبديل بـ PIN. Step-up بـ PIN لـ void وإدارة الموظفين (وبكلمة مرور لتغيير كلمات/أدوار).
- **UX:** عامل الوردية يلمس لوحة أرقام فقط؛ المدير يدخل مرة بجهازه.
- **أمن:** يغلق AUTH-01/02 + الانتظار-الطويل-بلا-تأكيد؛ يبقي localStorage/12h إلا إذا دُمج مع (3).
- **الوردية:** ثوانٍ إضافية فقط عند step-up النادر.
- **الأجهزة المشتركة:** نموذج «فتح الجهاز + PIN» يطابق الواقع حرفياً.
- **التعافي:** مدير يعيد إصدار PIN؛ الرموز القديمة تعمل حتى 12h (فترة سماح).
- **الهجرة:** إعادة إصدار كلمات مرور الوردية + PINs أطول عند الدخول التالي؛ رموز الحالية تبقى صالحة (توافق tv).
- **النطاق:** متوسط (backend: تحديات step-up + LoginAttempt + قيود؛ frontend: شاشة فتح جهاز + نافذة PIN).
- **مخاطرة:** تعقيد UX إذا صُمم «فتح الجهاز» بشكل متصلب؛ يجب أن يكون اختيارياً للجهاز الواحد.

### الخيار 3 — «جلسات جانب-خادم + أجهزة» (يغلق MF-18/19)
- **المعمارية:** فوق الخيار 1 أو 2: `AuthSession`/جهاز لكل جلسة؛ رمز وصول قصير (دقائق) + refresh بتدوير للهواتف الشخصية؛ جلسة جهاز طويلة (بلا idle قسري) لـ POS/KDS معروفة باسمها؛ خروج بعيد/إبطال جهاز؛ idle-timeout للجلسات التفاعلية؛ مدير يرى «المتصلين الآن».
- **UX:** الموظف لا يلحظ شيئاً إلا انتهاء الخمول في الهاتف الشخصي؛ المدير يكسب لوحة جلسات.
- **أمن:** يغلق سرقة الرمز طويلة الأمد، الجهاز المسروق، والخروج الخشن.
- **الوردية:** شاشات الوردية معفاة من الخمول عمداً (بلا أثر).
- **التعافي:** إبطال جهاز بعينه؛ إدارة كاملة.
- **الهجرة:** فترة سماح 12h للرموز القائمة (أو نشرها قبل الإطلاق حيث لا مستخدمين)؛ توافق tv كامل.
- **النطاق:** كبير (backend: جدولان + middleware تحقق جلسة؛ frontend: تجديد رمز + لوحة جلسات).
- **مخاطرة:** كل طلب يتحقق من الجلسة في DB (كما يفعل tv اليوم — لا تراجع أداء)؛ تعقيد تشغيلي أعلى (تنظيف الجداول، مراقبة).

### الخيار 4 — «تحصين القمة: MFA/Passkey للمنصة والمدراء فقط» (مكمّل لأي خيار)
- **المعمارية:** TOTP اختياري ثم إلزامي لـ PLATFORM/SUPER_ADMIN؛ passkey اختياري للمدير؛ لا MFA لأي وردية إطلاقاً.
- **UX:** إعداد لمرة واحدة؛ رمز 6 أرقام عند دخول المدير/المنصة.
- **أمن:** يحمي أعلى أهداف القيمة (عبر-المستأجرين، إيقاف مستأجرين، الاشتراكات).
- **الوردية:** صفر أثر.
- **التعافي:** رموز backup مُدقَّقة.
- **الهجرة:** تفعيل تدرّجي للمدراء الحاليين.
- **النطاق:** متوسط (توثيق + واجهات تفعيل + middleware عامل ثانٍ).
- **مخاطرة:** إغلاق مدير نفسه خارج MFA — يتطلب مسار backup صارماً.

> **«Owner approval required before implementation.»** — لا يُختار نهائياً هنا؛ إطار القرار في §23.

---

## 20) Implementation Impact Map — خريطة الأثر (كل مسار يتأثر بأي إعادة تصميم)

**Frontend:** `src/components/auth/LoginModal.tsx` (كل مسارات الدخول) · `src/context/AuthContext.tsx` (الجلسة، الأدوار، lockout، restore) · `src/services/api.ts:534-652` (الرمز، Bearer، logout، **لا 401 interceptor — يضاف**) · `src/components/manager/StaffManagement.tsx:135-180` (إصدار الأسرار — AUTH-01) · `src/components/manager/ManagerLayout.tsx` (حراس التبويبات) · شاشات step-up جديدة (نُدل PIN/كلمة مرور) · `src/types/restaurant.ts` (RestaurantUser/TenantRole).
**Backend:** `server/routes/auth.ts` (login/pin/logout/me/501) · `server/middleware/auth.ts` (signToken/authenticateToken/requireRole) · `server/middleware/rateLimit.ts` (LoginAttempt) · `server/routes/manager.ts:2395-2600` (إنشاء/تحديث/حذف موظفين + tv) · `server/routes/admin.ts` (منصة) · `server/validation/schemas.ts:324-381` (login/pin/staff) · `server/services/audit.ts` · `server/config.ts` (أعمار الرموز/السياسات).
**قاعدة البيانات:** `prisma/schema.prisma` (RestaurantUser:181 + كيانات §13) + هجرات جديدة (لا تنشأ الآن).
**البذور/الاختبارات:** `e2e/seed-test-data.mjs` · `src/tests/*` (auth/rbac/security — إعادة حزمة TEST 2) · `e2e/employee-functional-test.mjs` · `e2e/live-preview/*` · `src/tests/production-hardening.test.ts:646` (SSE) · pos.test.ts (سخرية وضعتMF-02 سابقاً).
**البنية:** `.github/workflows/` (بوابة CI — MF-04) · `Dockerfile`/`docker-compose.yml` (متغيرات جديدة) · `render.yaml` (env) · `README`/أدلة النشر (MF-16).

---

## 21) Risks — مخاطر مرحلة المعالجة نفسها

1. **كسر سرعة الوردية بـ«تحسين» أمني** — أعلى خطر تشغيلي؛ أي تغيير يُقاس بزمن تبديل موظف على جهاز مشترك.
2. **انحدار عزل المستأجرين** — 30/30 اليوم؛ لمس auth.ts/manager.ts يهدد المحصلة → إعادة حزمة TEST 2 شرط بوابة لكل PR.
3. **قفل المدراء خارج النظام** (MFA/كلمات إلزامية بلا مسار تعافٍ مُختبَر) — يجب اختبار التعافي قبل الإلزام.
4. **تضخم النطاق** (الخيار 3 كاملاً قبل الإطلاق) — يهدد الجدول؛ التقسيم المرحلي (P1 الآن، جلسات لاحقاً) أرشد.
5. **انحدار أداء** (فحص جلسة لكل طلب) — محايد إذا استُبدل فحص tv الحالي بمثله، أسوأ إذا تراكب الاثنان بلا تصميم.
6. **توافق الرموز القائمة** — قرار فترة السماح (§14-3) يجب أن يكون صريحاً لا ضمنياً.

---

## 22) Open Questions Requiring Owner Decisions

1. هل بيع الكاونتر (Walk-in) ضمن نطاق الإطلاق؟ (يحدد مصير MF-02: إصلاح vs حظر الخيار)
2. هل يُقبل «شبه لحظي ≤10 ثوانٍ» (استطلاع) حتى إصلاح SSE، أم الإصلاح شرط إطلاق؟
3. **أي خيار مصادقة (1–4، أو تركيبة 1+4 الآن و2+3 لاحقاً)؟**
4. هل تُفعَّل MFA إلزامياً لمنصة المنصتين؟ (موصى به تحليلياً — قرار مالك)
5. سياسة PIN الدنيا: 4 أرقام مع قفل حساب، أم ترقية إلزامية إلى 6؟
6. هل يحتاج الكاشير step-up على void، أم يكفي القفل الحسابي وسجل التدقيق؟
7. من يملك تسجيل الأجهزة الموثوقة — المدير فقط أم مسؤول وردية؟
8. فترة سماح الرموز القائمة عند أي تغيير جلسات: 12h أم إبطال فوري؟ (الإبطال الفوري مقبول **فقط** قبل الإطلاق)
9. أفق التوسع متعدد النسخ (MF-15): هل Redis في الخارطة قبل المعالجة أم بعدها؟

---

## 23) Recommended Decision Framework — إطار القرار (بلا فائز)

القاعدة الحاكمة: **«هوية قوية + جلسة تشغيل سريعة + step-up للأفعال الحساسة»** (§19 في المواصفة) — يتحقق بأي تركيبة تشبع **كل** الشروط التالية بلا استثناء:
1. عامل الوردية يكمل دخوله/تبديله في **≤5 ثوانٍ** على جهاز مشترك (لوحة أرقام فقط).
2. كل حساب له **ميزانية خطأ لكل حساب** (قفل تدريجي) — لا يعتمد الأمن على IP.
3. **لا سر مشتق من سر آخر** في أي مكان (AUTH-01 لا يتكرر).
4. الأفعال المالية (void) وإدارة الموظفين تتطلب **تأكيداً حديثاً** من صاحب الصلاحية.
5. أدوار المنصة خلف **عامل ثانٍ** (على الأقل اختياري TOTP).
6. الجلسات الطويلة مقصورة على **أجهزة معروفة/مسمّاة**، والهواتف الشخصية قصيرة الخمول.
7. كل حدث أمني (تغيير دور/سر/حالة) **يُبطل فعلياً ولا ينتظر** — كما يفعل tv اليوم (لا تراجع).
8. عزل المستأجرين 30/30 **يبقى أخضر** بعد التغيير (بوابة TEST 2).
9. كل مسار تعافٍ **مُدقَّق ومُختبَر** — لا استعادة بلا هوية.
10. خطة هجرة تصريحية: **رموز اليوم إما معفاة 12h أو تُبطل كلها قبل الإطلاق**.

الخيارات 1/2/4 تشبع 1–5 و7–9 بإنتاجية عالية؛ الخيار 3 يضيف 6 ويغلق MF-18/19 بكلفة نطاق أعلى — **الترتيب الزمني بينها قرار المالك.**

---

## 24) Repository Integrity Verification — تحقق سلامة المستودع

- `git status --short`: **لا ملفات معدلة ولا staged** — فقط غير متتبَّعة (untracked): تقارير التدقيق الأربعة السابقة + هذا التقرير + `e2e/prisma/` و`e2e/server.env` (أدوات بيئة الاختبار من سلسلة TEST، خارج الشجرة المتتبَّعة)
- `git diff` على كل المتبوع: **فارغ** · `git log -1`: `62b2f31` (رأس main كما بدأنا — لم تُنشأ فروع ولا commits ولا PRs)
- لم تُنفَّذ أوامر «إصلاح» ولا هجرات ولا `prisma generate/...` على المخطط؛ كل الأوامر كانت قراءة (grep/sed/cat/git read-only)
- الأدلة المؤقتة في `/tmp` (خارج المستودع) كما في كل سلسلة الاختبار

---

```
ANALYSIS_COMPLETE=YES
CODE_MODIFIED=NO
SCHEMA_MODIFIED=NO
MIGRATIONS_CREATED=NO
AUTH_CHANGED=NO
TESTS_MODIFIED=NO
CI_MODIFIED=NO

P1_FINDINGS=7
P2_FINDINGS=14
P3_FINDINGS=11

AUTH_REDESIGN_REQUIRED=YES
AUTH_OPTIONS_ANALYZED=4

TEST1_STATUS=CONDITIONAL_PASS
TEST2_STATUS=CONDITIONAL_PASS
TEST3_STATUS=CONDITIONAL_PASS
TEST4_STATUS=CONDITIONAL_PASS

PROPOSED_REMEDIATION_READY=YES
WAITING_FOR_OWNER_APPROVAL=YES
```

**التوقف هنا. لا يبدأ أي تنفيذ قبل موافقة صريحة من المالك على الخيار المعماري والمدخلات المفتوحة (§22).**
