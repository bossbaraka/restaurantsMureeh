# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.env
.env.*
!.env.example
backups/*.sql
uploads/*

# Local design-preview harnesses (e.g. `preview/welcome` renders the QR splash
# against stub tenant palettes so the theming can be eyeballed without a DB).
# Local only — nothing in here is part of the shipped app.
preview/

# Local debugging / throwaway scripts. NEVER commit: these historically
# carried live database connection strings (audit C-01). Anything needing
# database access must read process.env.DATABASE_URL with no fallback.
scratch/
# Local run environment for the e2e/ harness (contains the test database URL
# and a throwaway JWT secret). Committed templates live in e2e/*.example.
.e2e-scratch/
*.credentials
*.secrets
secrets.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
