# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.env
.env.*
!.env.example
backups/*.sql
uploads/*

# Local debugging / throwaway scripts. NEVER commit: these historically
# carried live database connection strings (audit C-01). Anything needing
# database access must read process.env.DATABASE_URL with no fallback.
scratch/
*.credentials
*.secrets
secrets.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
