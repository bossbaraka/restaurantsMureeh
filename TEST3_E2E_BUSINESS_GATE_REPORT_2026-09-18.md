# TEST 3 — REAL RESTAURANT END-TO-END GATE (E2E BUSINESS REPORT)
**المشروع:** restaurantsMureeh (MÉRAR SaaS) · **التاريخ:** 2026-09-18 · **الأدوار:** Senior QA / Restaurant SaaS Product QA / E2E Engineer / Business Process Analyst
**البيئة:** Node 22 + Express + PostgreSQL 16 (مضمن) + React SPA — بذرة اختبار لـ 3 مستأجرين و7 أدوار، ثم **إعادة تشغيل فعلية للخادم في منتصف السيناريو** لاختبار الثبات.
**المنهجية:** 104 فحصاً عبر دورة حياة كاملة لخدمة عشاء حقيقية: إعداد المطعم → دخول الزبون عبر QR → طلب بخيارات وإضافات واستثناءات → نداء نادل → طلب الحساب → تحصيل الكاشير → المطبخ (تذكرة → تقديم) → تتبع الزبون → لوحة الإدارة → تدقيق قاعدة البيانات. السجل الكامل: `/tmp/e2e3-final.log` + لوق الخادم.

---

## الحكم الإجمالي: **CONDITIONAL PASS**

دورة العمل الأساسية للمطعم (طاولة → طلب → دفع → مطبخ → تقديم) تعمل من طرف إلى طرف بسلامة مالية وتشغيلية عالية — لكن هناك **عائقان من فئة P1** يمنعان الإطلاق: **بيع الكاونتر (Walk-in) معطّل بـ 500**، و**التحديث اللحظي لا يصل للمتصفحات الحقيقية** (gzip). لا توجد أي حالة مستحيلة في قاعدة البيانات (13/13 فحص اتساق).

**النتيجة الكمية: 102/104 فحصاً ناجحاً** — الإخفاقان هما العيبان الحقيقيان الموثقان أدناه.

---

## مصفوفة السيناريوهات (Scenario Matrix)

| السيناريو | النتيجة | الدليل | الخطورة |
|---|---|---|---|
| Restaurant setup | ✅ PASS | 15/15: onboard بمدير منصة (201 + صف DB)، رفع شعار/غلاف (فحص magic bytes)، Branding كامل (ثيم/عملة ILS/لغة/توقيت)، تصنيف + منتج بأحجام و3 إضافات ومكونات قابلة للحذف، طاولة + QR + إعادة توليد QR، **الثبات بعد إعادة تحميل الصفحة وإعادة تشغيل الخادم** (1.13–1.15) | — |
| QR entry | ✅ PASS | 2.01–2.11: QR → مطعم → طاولة → جلسة → قائمة؛ QR غير صالح/مشوّه → 404 برسالة عربية صادقة (لا رسالة مضللة أثناء التحميل)؛ جلسة منتهية → 403؛ SPA بـ RTL/عربي/viewport جوال | — |
| Menu | ✅ PASS | الكتالوج يعرض الأحجام (2) والإضافات (3) والمكونات القابلة للحذف (2) والبرanding؛ مطعم فارغ → قائمة فارغة بدون خطأ | — |
| Order | ✅ PASS | 15/15 (3.01–3.15): تسعير خادمي دقيق (وحدة 62+15 حجم+8 إضافات ×2 + 12 = **182** مطابقة تماماً)؛ لقطات سعرية بالاسم/الحجم/الإضافات/الاستثناءات/التعليمات؛ كمية 0/51 مرفوضة؛ منتج/حجم غير معروف → 400؛ أسعار العميل مُتجاهَلة؛ **إعادة الإرسال بنفس clientRequestId لا تُكرر التذكرة**؛ إلغاء الزبون لطلبه يعمل | — |
| KDS | ✅ PASS | **بوابة الدفع-قبل-التنفيذ تعمل**: طلب الزبون يُنشأ محجوزاً `AWAITING_PAYMENT` ويظهر للمطبخ `operational=false`، ومحاولة البدء → 409 «بانتظار التحقق من الدفع». بعد التحصيل: `operational=true` ثم PENDING→PREPARING→READY→SERVED؛ التكرار idempotent؛ التخطي/الرجوع/إحياء SERVED → 409؛ تتبع الزبون يعكس كل حالة؛ تذاكر متعددة عبر طاولات | — |
| Waiter | ✅ PASS | 5.01–5.09: إنشاء النداء، جدولة مزدوجة → 429 (نداء نشط واحد + cooldown 45 ثانية)، آلة حالات خطية ACK→RESOLVED، طلب الحساب بعد انتهاء الـ cooldown → 201 + الطاولة → BILL_REQUESTED + ظهوره للطاقم | — |
| Bill | ✅ PASS | تجميع جولتين على نفس الجلسة (182+67=249)؛ **تغيير سعر المنتج 62→95 لا يرجع على الطلبات التاريخية** (order.total=182 و unitSnapshot=85 لم يتغيرا) والطلب الجديد يستخدم 95 | — |
| POS | ❌ **FAIL** | **بيع الكاونتر `__WALKIN__` → 500** (خرق مفتاح أجنبي `Order_tableId_fkey`) — انظر P1-A. خدمة الطاولات عبر POS تعمل | **P1** |
| Payment | ✅ PASS (خدمة الطاولات) | نقدي مع حساب الباقي والإكرامية (249 نقد 364 → باقٍ 105 + إكرامية 10)، بطاقة، محفظة، نقص النقد → 400، **إعادة الدفع/الإعادة → 409**، إبطال إيصال يعيد الطلب UNPAID ويمكن تحصيله مجدداً | — |
| Receipt | ✅ PASS | ترقيم تسلسلي فريد لكل مطعم `RC-2026-0001…0006` (بلا فجوات)، دفتر لا يُحذف (الإبطال علامة)، كل إيصال = مجموع طلباته بالضبط | — |
| Realtime | ❌ **FAIL (للمتصفحات)** | البث يعمل مثالياً مع `Accept-Encoding: identity`: حدث الطلب لتبويبين + طاقم، نداء النادل، الدفع + RELEASED، تغيير الحالة، **إعادة الاتصال تستقبل الأحداث الجديدة**، نبضة قلب 25ث. لكن مع gzip (كل المتصفحات): **0 بايت** — انظر P1-B | **P1** |
| Analytics | ✅ PASS | لوحة الإدارة: الإيرادات **تطابق مجموع الطلبات في DB حتى القرش** (448=448)، عدّادات الحالات، تصدير الطلبات | — |
| Multi-branch | ✅ PASS | CRUD فروع كامل للمدير المخوّل (201/200/200/200) + ربط طاولات بفرع (branchId في تحديث الطاولة) | — |

**قيود الأدوار (Phase 9):** كاشير ↔ إنشاء منتجات 403؛ نادل ↔ دفتر المدفوعات 403؛ مطبخ ↔ إنشاء طاولات 403؛ نادل ↔ إدارة موظفين 403 — كلها مفروضة من الخادم. ✅

---

## عوائق الإطلاق (Release Blockers — P0/P1 فقط)

### P1-A — بيع الكاونتر (Walk-in/Counter) في POS معطّل تماماً: 500
- **الخطورة:** P1 — سير عمل أساسي مكسور (المطعم يمكنه العمل بالطاولات فقط؛ البيع المباشر عند الكاشير مستحيل).
- **المسار:** `POST /api/manager/orders` بـ `tableId: "__WALKIN__"` — `server/routes/manager.ts:705`.
- **الجذر:** `Order.tableId` عليه مفتاح أجنبي إلى `Table`؛ `'__WALKIN__'` ليس طاولة → Prisma `P2003` → 500 «تعذر إنشاء فاتورة الكاشير». اللافت أن `Payment.tableId` (نص عادي) يدعم `__WALKIN__` بالتصميم والمسار يتحقق من `isWalkIn` في بدايته — أي أن النية دعم الميزة لكن الإدراج ينهار.
- **الأثر التشغيلي:** زر «عميل مباشر» موجود في واجهة الكاشير (`CashierPOSView.tsx:398`، يرسل `__WALKIN__` في `createManagerOrder`) — الكاشير يضغط فيحصل على toast خطأ. **اختبارات المستودع نفسه تمر فقط لأن `pos.test.ts` يسخر fetch ولا يلمس قاعدة البيانات الحقيقية** (سبب عدم اكتشافه في TEST 1).
- **الإصلاح المقترح (غير منفّذ — نطاق تدقيق فقط):** إما حجز صف Table اصطناعي `__WALKIN__` لكل مطعم، أو جعل `Order.tableId` يقبل قيمة العداد (عمود عرض + FK منفصل اختياري)، أو إنشاء طلب الكاونر عبر مسار خاص. بعدها: اختبار تكاملي حقيقي ضد DB.

### P1-B — التحديث اللحظي (SSE) لا يصل للمتصفحات الحقيقية (gzip buffering)
- **الخطورة:** P1 — قناة أساسية مكسورة لجميع العملاء الفعليين.
- **الجذر:** وسيط `compression()` يضغط `text/event-stream` ويخزّن الكتابات الصغيرة مؤقتاً دون flush لكل حدث (`server/index.ts` + `server/services/realtime.ts`).
- **الدليل في سياق العمل:** بثّ ضيف بـ `Accept-Encoding: gzip` (ما يرسله كل متصفح): طلب جديد على طاولته → **0 بايت خلال 2.5 ثانية** رغم توليد الحدث (201). نفس البث بـ `identity`: كل الأحداث فورية (8.03–8.07 كلها PASS). النبضة 8 بايت/25ث لن تملأ مخزن zlib إلا بعد ساعات — أي أن تتبع الطلب، شاشة المطبخ، نداءات النادل، وإشعارات الكاشير **صامتة عملياً** في المتصفح.
- **ملاحظة:** البنية نفسها سليمة (النطاق، الترشيح، إعادة الاتصال، النبضة) — العيب في طبقة الضغط فقط. (نفس النتيجة SEC-01 من TEST 2، مؤكدة الآن في سياق العمل.)
- **الإصلاح المقترح:** استثناء `text/event-stream` من الضغط أو `res.flush()` بعد كل بثّ.

---

## القضايا التجارية المستحيلة (Broken Business Invariants)

1. **«بيع مباشر عند الكاونتر» غير قابل للتحقق إطلاقاً** — الدليل (`Payment.tableId` موثق `__WALKIN__`) مقابل المستحيل فيزيائياً (`Order` FK). لا يمكن وجود طلب كاونتر واحد في النظام كله.
2. **«لا شاشة تحتاج تحديثاً يدوياً عند توقع اللحظية»** مكسورة لكل متصفح حقيقي (كل شاشات SSE تعمل polling ضمنياً أو لا ترى شيئاً).

**لا توجد أي حالة مستحيلة في البيانات**: تدقيق DB المباشر (10.01–10.13) — صفر يتامى (OrderItems/طلبات/جلسات/نداءات بلا آباء)، صفر تناقضات (PAID بلا settledAt/كاشير = 0، إيصال ≠ مجموع طلباته = 0، طلب تحت إيصالين حيّين = 0، ملغى إيصاله لكنه PAID = 0، PAID لكن محجوز = 0، طاولة BILL_REQUESTED بلا نداء = 0)، والدخل النهائي: 11 طلباً (1 مكتمل SERVED، 6 مدفوعة قيد المطبخ، 1 ملغاة، طلبا البذرة) و7 إيصالات (6 حية + 1 مبطّل) وكل الطاولات AVAILABLE.

---

## ملاحظات أدنى (P3 — لا تعيق الإطلاق)
1. **Cooldown 45 ثانية للنداءات يبقى بعد حلّ النداء** — زبون طلب نادلاً ثم أراد الحساب خلال 45 ثانية → 429. تصميم مقصود (debounce) لكنه قد يحرج في الذروة؛ يُستحسن إسقاط الـ cooldown عند RESOLVED.
2. **دفتر المدفوعات الافتراضي يُدرج الإيصالات المبطلة** (بعلامات) — مقبول للتدقيق، لكن قد يربك الكاشير؛ يُقترح فلتر افتراضي.
3. **قائمة `scope=operations` تعيد الطلبات المحجوزة مع `operational:false`** والواجهة ترشّحها — سليم، لكن تحميلها للعميل مع نية إخفائها قابل للنقاش.
4. F-04 من TEST 1 (نافذة دفع الزبون في live-preview) خارج نطاق هذا الاختبار — مسار الإثبات عبر API يعمل (TEST 2: P08)، والمشكلة كانت في العرض.

## نقاط قوة تجارية مكتشفة (إيجابية)
- **بوابة الدفع-قبل-التنفيذ**: المطبخ لا يطبخ مالاً غير محقق — الطلب محجوز حتى تحصيل/تحقق الكاشير، ثم يدخل كتذكرة جديدة. تعمل بدقة على كل الطبقات (DB/عرض/تحويلات).
- **Idempotency شامل**: إعادة إرسال طلب الزبون (clientRequestId)، إعادة الدفع (409)، تحويلات الحالة (CAS) — لا ازدواج مالي ممكن.
- **اللقطات السعرية**: تغيير الأسعار لا يمس التاريخ أبداً.
- **قدرات الجلسة**: بعد الدفع تُغلق جلسة QR لكن الزبون يبقى يتتبع طلبه (قراءة فقط) — تصميم مدروس.

---

```
E2E_GATE=CONDITIONAL_PASS
CORE_ORDER_FLOW=PASS
PAYMENT_FLOW=CONDITIONAL_PASS  # table service PASS; walk-in counter broken (P1-A)
REALTIME_FLOW=FAIL             # works only with Accept-Encoding: identity (P1-B)
DATA_CONSISTENCY=PASS          # 13/13 DB invariants, zero orphans/contradictions
BLOCKERS=2                     # P1-A walk-in POS 500 (FK), P1-B SSE gzip buffering
NEXT_GATE=TEST_4
```
