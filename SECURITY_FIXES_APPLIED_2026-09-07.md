# تقرير الإصلاحات الأمنية المطبقة — 2026-09-07

**النطاق:** معالجة نتائج `SECURITY_AUDIT_2026-09-07.md` (الحكم الأصلي: **NO LAUNCH** — 22/100).
**الفرع:** `arena/01a07a7b-restaurantsmureeh`
**المنهجية:** إصلاح دفاعي فقط — لا مساس ببيانات إنتاجية، وكل إصلاح مُثبت باختبار تشغيلي.

---

## 1. الخلاصة التنفيذية

طُبقت إصلاحات **الحرجة السبع (C-01..C-07)** كاملةً، ومعظم البنود العالية والمتوسطة:

| الفئة | قبل | بعد |
|---|---|---|
| أبواب خلفية (demo/PIN) | 3 أبواب نشطة | مُزالة كلياً من الخادم والواجهة |
| JWT | سر احتياطي مضمّن قابل للتزوير | سر إلزامي 32+ حرف، HS256 مثبّت، iss/aud، إبطال فوري |
| RBAC | معدوم (ترقية ذاتية لـ PLATFORM_ADMIN) | أدوار مطبقة على كل مسار + قائمة أدوار مسموحة |
| عزل المستأجرين | BOLA في النداءات والتصدير | JWT-first + فحص ملكية لكل صف |
| QR | تجاوز عبر default/ID/رقم + استبدال مستأجر | مطابقة دقيقة للتوكن العشوائي فقط |
| التسعير | أسعار من العميل + XSS مخزّن | تسعير كامل من قائمة قاعدة البيانات + escape |
| الرفع | أي ملف بأي امتداد + تنفيذ same-origin | مدير فقط + magic bytes + امتداد من الخادم |
| الدفع | نقد ناقص يُعلَّم مدفوعاً + سباقات | مبلغ كافٍ إلزامي + حجز مشروط + 409 عند التعارض |
| Rate limiting | صفر | 7 محددات مستهدفة (دخول/PIN/طلبات/نداءات/رفع...) |
| CORS/CSP | fail-open + CSP معطّلة | allow-list صارمة + CSP كاملة + fail-closed |

**الإثبات:** 47/47 فحص هجومي أخضر ضد الكود الجديد بنفس هجمات التدقيق (§5)، والبناء والاختبارات والـ lint خضراء (§6).

---

## 2. الإصلاحات الحرجة (P0 — كانت موانع إطلاق)

### C-01 — إزالة أبواب Demo/PIN الخلفية ✅
- `server/routes/auth.ts`: حُذف التوفير التلقائي لحسابات `demo@*`، وحُذف تجاوز كلمات `demo/demo123/123456/mureeh2026`، وحُذف التوفير التلقائي لرمو PIN السحرية (9900/1122/4455/7788) وترقية أي بريد يحوي `demo`.
- الدخول بـ PIN يتطلب `restaurantId` الآن، ومحصور بنطاق المستأجر النشط، ومدراء المنصة ممنوعون من دخول PIN (كلمة مرور فقط).
- `LoginModal.tsx`: حُذف زر 1-Click Demo وبطاقة بيانات demo، والتوجيه بعد PIN أصبح حسب **الدور** لا قيمة الرمز.
- مقارنة bcrypt غير متزامنة + dummy hash لتوحيد التوقيت ضد user-enumeration.

### C-02 — إصلاح تزوير JWT ✅
- `server/config.ts` (جديد): التحقق من البيئة عند الإقلاع — `JWT_SECRET` أقصر من 32 حرفاً = رفض إقلاع. **لا يوجد سر احتياطي.**
- `server/middleware/auth.ts`: تثبيت `HS256` + تحقق `issuer/audience` + فحص جديد من قاعدة البيانات لكل طلب (الحالة ACTIVE + تطابق `tokenVersion`).
- `GET /me` و `/logout` أصبحا يعملان (كانا 401 دائماً لغياب `authenticateToken` عن `/api/auth`).
- **كاسر للتوافق (مقصود):** كل التوكنات القديمة تُرفض — دوّر `JWT_SECRET` عند النشر.

### C-03 — تطبيق RBAC على الخادم ✅
- `requireManager()` لكل عمليات الكتابة الإدارية (طاولات/قائمة/موظفين/عروض/اشتراك/هوية/فروع).
- `requireCashierOrManager()` لنقاط البيع والدفع والتسوية وقراءة المدفوعات.
- `requireServiceStaff()` (مدير/كاشير/نادل/مطبخ) لتحديث حالات الطلبات والنداءات — تدفق KDS محفوظ.
- أدوار الموظفين محصورة بـ `RESTAURANT_MANAGER/WAITER/KITCHEN/CASHIER/STAFF` (مستحيل منح أدوار منصة).
- ردود الموظفين بـ `select` آمن — لا تسريب لـ `passwordHash/pinHash`.
- حماية آخر مدير + منع تغيير الدور/الحالة الذاتية + إبطال جلسات الهدف عند تغيير بياناته.

### C-04 — إغلاق BOLA ✅
- حلّال المستأجر JWT-first: غير المنصة يُحلّ دائماً لمستأجره من التوكن، وتُتجاهل `restaurantId` المتلاعب بها بأمان.
- فحص ملكية صريح أُضيف لـ `GET /waiter-requests` و `GET /export/orders` (كانا مفتوحين).
- إنشاء المنتج يتحقق أن التصنيف من نفس المستأجر؛ كل فحص مرفوض يُسجَّل في سجل التدقيق.

### C-05 — إغلاق تجاوز QR ✅
- الجلسة تتطلب مطابقة **دقيقة** للتوكن العشوائي — لا `default` ولا ID ولا رقم، ولا إنشاء طاولات ولا استبدال مستأجرين.
- حُذف التعيين `merar/marer→mureeh` وإعادة التفعيل التلقائي للموقوفين؛ الكتالوج والجلسات للمطاعم ACTIVE فقط.
- `cancel/notes` مربوطة بجلسة الطلب نفسه (`order.sessionId`)، والتوكن في body الـ POST لا في URL.
- توكنات QR/JWT جديدة كلها CSPRNG عشوائية بالكامل.

### C-06 — تسعير من الخادم + إغلاق XSS ✅
- الطلبات العامة تُسعَّر بالكامل من قائمة قاعدة البيانات: سعر أساسي + حجم (ID أو مطابقة اسم دقيقة) + إضافات (IDs تُتحقق بصرامة) — أسعار العميل تُتجاهل، والمنتج الغريب/المجهول/غير المتوفر = `400`.
- اللقطات (`productNameSnapshot`...) من قاعدة البيانات لا من العميل.
- فواتير `OrderManagement` وإيصالات `CashierPOSView`: escape كامل + إزالة `<script>` المضمّن (الطباعة من النافذة الأصلية).
- تصدير CSV: BOM للعربية + تحييد حقن المعادلات (CWE-1236) + اسم ملف معقّم.

### C-07 — تحصين الرفع ✅
- `requireManager()` + حد 60/ساعة + تخزين ذاكرة (الفحص قبل لمس القرص).
- فحص magic bytes حقيقي (PNG/JPG/WEBP/GIF) — رفض `text/html` المتنكر.
- اسم ملف وامتداد مولّدان من الخادم — لا سيطرة للعميل على نوع المحتوى المقدَّم.
- تقديم `/uploads` بترويسات `nosniff` + `CSP: sandbox` دفاعاً معمّقاً.

---

## 3. الإصلاحات العالية (P1)

| البند | الإصلاح |
|---|---|
| H-01 SSE | كل البث أصبح table-scoped (لا تسرب بين الطاولات)؛ مسار JWT للموظفين بتحقق صارم + تنقيح التوكن من السجلات؛ سقف 2000 عميل؛ الواجهة أزالت JWT من URL |
| H-02 الجلسات | `tokenVersion` في قاعدة البيانات: الخروج/كلمة المرور/PIN/الدور تُبطل التوكن فوراً؛ `api.logout()` يستدعي الخادم أولاً ثم يمسح محلياً |
| H-03 سلامة المراجع | فحص ملكية التصنيف عند إنشاء/نقل المنتج؛ منع حذف تصنيف فيه أطباق ومنتَج مرتبط بفواتير (409 مع إرشاد) |
| H-04 الدفع | طريقة دفع enum؛ كل فاتورة مربوطة بطاولتها؛ النقد يتطلب مبلغاً كافياً؛ حجز مشروط `UNPAID` + فحص العدد (409 عند السباق)؛ إعادة توليد الإيصال عند التعارض؛ الواجهة ترسل المقبوض (والمطابق التلقائي للدفع الدقيق) |
| H-05 CORS/CSP | CORS بـ allow-list دقيقة + fail-closed في الإنتاج؛ CSP كاملة (تشمل `frame-ancestors` قابلة للضبط)؛ حد JSON خُفّض 10MB→1MB؛ رسائل parser عامة |
| H-06 Rate limits | 7 محددات (أعلاه) — بلا محدد عام حتى لا ينكسر الـ polling المشروع (8 طلبات/1.5ث) |
| H-07 النشر | إزالة `db push --accept-data-loss` من الإقلاع والسكربتات؛ PostgreSQL على `127.0.0.1` فقط في compose |
| H-08 الباقات | فرض حدود الطاولات/التصنيفات/الأطباق من الخادم + `CAN_EXPORT_REPORTS` للتصدير + `CAN_CREATE_BRANCH` للفروع؛ `checkEntitlement` في الواجهة حقيقية (تلميح فقط — الخادم هو المُنفّذ) |

## 4. إصلاحات متوسطة/منخفضة

- M-01 تحقق zod شامل لكل الأجسام (strict — رفض الحقول المجهولة/المميزة).
- M-02 كلمات المرور 8+ أحرف؛ PIN رقمي 4-10؛ `crypto.getRandomValues` لتوليد PIN الواجهة؛ bcrypt غير متزامن (12 للكلمات، 10 للـ PIN).
- M-03 تنقيح `?token=` من سجلات الوصول؛ إلغاء التوكن من URL في SSE العملاء و cancel.
- M-04 ترقيم الطلبات/الإيصالات مع إعادة محاولة عند التعارض.
- M-05 `password-reset-request` يرد `501` صريحاً بدل النجاح الكاذب (لا يوجد مرسل بريد).
- M-06 بوابة `SAAS_E2E_SEED=1` على seeding الاختبارات (فحص اتصال للقراءة فقط افتراضياً).
- M-07 النسخ الاحتياطي: `execFile` بلا shell + رفض العمل بدون `DB_PASSWORD` + ملفات `0600`.
- M-08 إزالة متغيرات وبقايا الـ demo من `render.yaml`/`.env.example`.
- L-01/L-02 إزالة `Math.random` من كل المسارات الأمنية (توكنات/جلسات/PIN/ملفات)؛ SSE للعملاء بجلسة QR فقط.

---

## 5. الإثبات التشغيلي (47/47 ✅)

بُني harness مؤقت (حُذف بعد التشغيل): حُزمت مسارات الخادم **الحقيقية** مع Prisma وهمية بمستأجرين A/B، ونُفذت نفس هجمات التدقيق:

- C-01: demo login=401 بلا توفير، PIN بلا نطاق=400، PIN سحري=401 بلا توفير، PIN شرعي=200، PIN بمستأجر خاطئ=401.
- C-02: توكن مزور بالسر القديم=401، سر خاطئ=401، شرعي=200.
- C-03: ترقية ذاتية=403 والدور ثابت، منح PLATFORM=400، إنشاء بدور منصة=400، إنشاء شرعي=201 بلا hashes.
- C-04: صفر صفوف من B في النداءات والتصدير.
- C-05: `default`/ID/رقم=404، توكن دقيق=200، slug مجهول=404.
- C-06: إجمالي 100 من قاعدة البيانات رغم `unitPrice: 0.01`، منتج أجنبي/مجهول=400.
- C-07: HTML=400، امتداد مموّه يُفرض `.png`، نادل=403.
- H-02: دخول/me/logout=200 ثم التوكن=401، كلمة خاطئة=401، موقوف=403/401.
- H-04: طلب POS=201 (50)، نقد ناقص=400 والطلب UNPAID، دفع كافٍ=201 والباقي 10، دفع مكرر=409، طريقة مجهولة=400، نادل=403.
- RBAC: حالة مجهولة=400، النادل **يستطيع** تحديث الحالة (KDS يعمل)، عبر-المستأجر=404.

## 6. فحوص الجودة

- `npm run build` (tsc + vite): ✅ ناجح.
- `npm test`: ✅ 29 ناجح / 1 متخطى (كخط الأساس).
- `npm run lint`: ✅ 0 أخطاء (144 تحذيراً مقابل 156 قبل — كلها مسبقة).
- `node --check security-tests/api-security-smoke.mjs`: ✅.
- `security-tests/api-security-smoke.mjs` حُدثت للدلالات المصلحة (ثابت tenant-A جديد مطلوب) — شغّلها ضد PostgreSQL مؤقتة قبل الإطلاق.

## 7. ما تبقى قبل الإطلاق (خارج هذا الإصلاح)

1. تشغيل `security-tests/` ضد PostgreSQL 17 مؤقتة بمستأجرين اصطناعيين (لا يمكن هنا لغياب قاعدة بيانات).
2. تدوير `JWT_SECRET` في الإنتاج + إبلاغ المستخدمين بإعادة الدخول.
3. `npx prisma db push` (بدون flags مدمرة) لتطبيق `tokenVersion`/`KITCHEN`.
4. مخزن Redis مشترك للـ rate limits عند التوسع لأكثر من نسخة.
5. تكامل دفع حقيقي للترقيات المدفوعة + مرسل بريد لاستعادة كلمة المرور.
6. مراجعة بشرية للـ diff قبل الدمج.

---

*أُعدّت هذه الوثيقة من الكود الفعلي ونتائج التشغيل الفعلية — لا من التخمين.*
